# Automated Runtime Report

**Goal**: unattended validation of the installed `codex-vl` package

## Target

- platform:
- package version:
- install source:
- test date:

## Status

- `PASS` or `FAIL` package/version check
- `PASS` or `FAIL` top-level help
- `PASS` or `FAIL` subcommand help routing
- `PASS` or `FAIL` login status
- `PASS` or `FAIL` MCP listing
- `PASS` or `FAIL` feature listing
- `PASS` or `FAIL` completion generation
- `PASS` or `FAIL` app-server schema generation
- `PASS` or `FAIL` non-interactive execution
- `PASS` or `FAIL` workspace read/write checks
- `PASS` or `FAIL` network smoke
- `PASS` or `FAIL` `/loop` unattended lifecycle checks
- `PASS` or `FAIL` installed binary/package sanity
- `PASS` or `FAIL` no-crash runtime surface

## Commands or checks run

- 

## Result

- summary:

## Notes

- 
