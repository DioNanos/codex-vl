# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-06-03
- Platform: macOS (Apple Silicon)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` 0.136.0
- Commit under test: `eb2c5749f2`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and clean before report write.
- HEAD is `eb2c5749f2` (`docs(readme): next tracks 0.136.x (0.136.1); @next install; Android bundles libc++_shared.so`).
- Installed binaries report `0.136.0`.
- `npm dist-tags` returned `latest: 0.135.0`, `next: 0.136.1`, and platform-specific next tags for `darwin-arm64` and others.

Verdict: PASS (release-candidate binary/version checks are coherent for the installed `0.136.0` build).

## 2. Package And Command Surface

Checks executed from installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.136.0`
- `codex-vl-exec --version` -> `codex-exec 0.136.0`
- Major help routes returned successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned a readable logged-in state without secret leakage.
- `codex-vl mcp list` displayed configured servers with sensitive values redacted.
- `codex-vl features list` rendered the expected feature inventory.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- plan management
- interactive TUI/session control

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temp workspaces:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Exact `OK` response received from the non-interactive exec path.
- Directory/read listing returned the expected temp workspace contents.
- Write/read test succeeded with `seed` and `hello-codex-vl` returned in order.
- Network check failed with environment-level DNS resolution error (`Could not resolve host: www.google.com`), not a routing or runtime crash.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version, model, directory, and permission mode.
- Vivling/chat surfaces appeared during startup.
- Slash-command interaction was attempted, including `/loop ls` and goal/navigation checks, but the TUI transcript was ANSI-heavy and not cleanly observable as plain text in this terminal session.
- The session shut down cleanly on interrupt and printed `Shutting down...`.

Limitations:

- The interactive surface is not well suited to line-by-line transcript capture in this terminal mode.
- I did not force a synthetic success for `/loop`, `/goal`, `/vivling`, or `/vl` beyond the observable startup and shutdown behavior.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke in a temporary workspace with `pwd`, file listing, and readback.
- Runtime/write surface: verified via `codex-vl-exec` file creation and content readback.
- Patch surface: this report file was created via the patch workflow and then reviewed as a normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- Environmental DNS resolution failure during the external network smoke.
- Interactive TUI transcript capture is noisy in this terminal, so slash-command verification is only partially observable.
- No core command-surface or installed-runtime regression found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: core CLI/runtime surfaces and read/write behavior passed on macOS for `0.136.0`; observed issues were environmental (DNS) and terminal transcript noise, not binary failures.
