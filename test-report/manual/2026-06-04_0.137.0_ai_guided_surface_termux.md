# Codex-VL 0.137.0 Termux AI-Guided Surface Report

- Date: 2026-06-04
- Repo: `<repo>`
- Branch: `develop`
- Commit under test: `caa51a907a1cc12dbfc54bc0fc9d9e60ee93d850`
- Installed CLI versions: `codex-vl 0.137.0`, `codex-vl-exec 0.137.0`
- Validation mode: manual AI-guided checks, one command at a time

## Environment Summary

- Platform: Android/Termux on the mobile command center
- Workspace: temporary throwaway directories for runtime smoke
- Network: available for the smoke test
- Notes: `/tmp` was not writable in the shell context used here, so writable temp files were placed under `<repo>`-adjacent home paths instead

## Repo And Release State

- PASS: `git fetch --all --prune` and `git pull --ff-only` completed cleanly on `develop`
- PASS: branch was tracking `origin/develop`
- PASS: HEAD matched the intended release candidate for `0.137.0`
- PASS: release metadata in the repo pointed at `0.137.0`

## Command Surface

- PASS: `codex-vl --version` reported `codex-cli 0.137.0`
- PASS: `codex-vl-exec --version` reported `codex-exec 0.137.0`
- PASS: `codex-vl --help`, `exec --help`, `review --help`, `login --help`, `logout --help`, `resume --help`, `fork --help`, `mcp --help`, `sandbox --help`, `app-server generate-json-schema --help`, and `debug prompt-input --help` all rendered help text
- PASS: `codex-vl login status` returned a readable login state
- PASS: `codex-vl mcp list` showed configured MCP servers with secrets redacted
- PASS: `codex-vl features list` rendered the feature inventory
- PASS: `codex-vl completion bash` worked once written to a writable location

## Tool Surface

- PASS: shell command execution was available
- PASS: stdin/session continuation was available
- PASS: patch application was available
- PASS: loop management tooling was available in the workspace
- PASS: MCP discovery via the CLI surface was available

## Runtime Smoke

All checks were run manually in disposable workspaces.

- PASS: `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'` returned exactly `OK`
- PASS: `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'` read the workspace and listed files correctly
- PASS: `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'` created the file and read both contents back
- PASS: `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'` returned `HTTP/2 200`

## Manual TUI Checks

- PASS: first launch rendered the version card, model, working directory, and permission mode
- PASS: the inline TUI stayed up without crashing on startup
- PASS: `/loop` surfaced its command help
- PASS: `/goal` accepted goal text and updated the session view
- PASS: `/goal clear` was exercised to clear state
- PASS: `/vivling` and `/vl` were exercised; no crash or fatal UI break was observed
- Limitation: inline terminal capture is noisy with escape sequences, so the TUI output was not suitable for a clean transcript, but the interactive surfaces did respond

## Read, Write, And Patch Behavior

- PASS: shell/read surface worked in a temporary workspace
- PASS: runtime/write surface created and read back `hello.txt`
- PASS: patch surface was used to add this report file
- PASS: `git diff --check` was clean after the patch
- PASS: `git status --short` showed only the intended report change

## Failures, Blockers, Residual Risk

- No blocking functional failures were observed in the installed CLI or runtime smoke
- Minor environmental limitation: `/tmp` was not writable from the shell context used for this validation, so writable temp files were placed elsewhere
- Residual risk: TUI capture was noisy, so the report relies on direct manual observation rather than a clean transcript

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

