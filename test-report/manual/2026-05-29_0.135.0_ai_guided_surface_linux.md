# Codex-VL AI-Guided Surface Report (Linux)

- Date: 2026-05-29
- Platform: Linux (x86_64)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` 0.135.0
- Commit under test: `46fa9476fc`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and clean before report write.
- HEAD is `46fa9476fc` (`test-report: add 0.135.0 macOS AI-guided surface validation`).
- Installed binaries report `0.135.0`.
- npm dist-tags returned `latest: 0.134.1` and `next: 0.135.0`.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.135.0`.
- `codex-vl-exec --version` -> `codex-exec 0.135.0`.
- All requested help routes returned successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned logged-in state without secret leakage.
- `codex-vl mcp list` displayed configured servers with sensitive env values redacted.
- Feature inventory rendered and included expected runtime capability families.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- MCP/app tool discovery and callable tool interfaces

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temp workspace `<tmp-workspace>`:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt ... read seed.txt and hello.txt ...'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com ...'`

Evidence summary:

- Exact `OK` response received from `codex-vl exec`.
- All `codex-vl-exec` smoke checks failed in this environment with:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- Failures occurred before successful read/write/network execution.

Verdict: FAIL (environment prevented required runtime smoke from completing).

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Evidence summary:

- First launch rendered startup surface (version, model, directory, permission mode).
- MCP startup status and status-line surfaces were visible.
- Slash-command interaction was attempted in-session; terminal capture was ANSI-frame heavy.
- Session ended cleanly with `Shutting down...`.

Verdict: PASS WITH OBSERVABILITY LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: pass from command-surface checks.
- Runtime/write surface: blocked during `codex-vl-exec` smoke by `bwrap` loopback error.
- Patch surface: this report file was added through patch workflow and staged as normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: FAIL (runtime write path could not be validated).

## Failures, Blockers, Residual Risk

- `codex-vl-exec` workspace-write smoke blocked by host environment restriction (`bwrap` loopback RTM_NEWADDR operation not permitted).
- Because runtime read/write smoke is a release gate, this Linux run cannot approve release readiness by itself.
- TUI transcript quality was degraded by ANSI-heavy output in this terminal capture mode.

## Final Decision

FAIL

Rationale: command surface and basic interactive startup are healthy, but required installed runtime smoke (`codex-vl-exec` read/write path) did not pass in this Linux environment.
