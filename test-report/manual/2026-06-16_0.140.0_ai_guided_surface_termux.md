# Codex-VL AI-Guided Surface Validation (Termux)

- Date: 2026-06-16
- Decision: PASS WITH ENVIRONMENTAL LIMITATION
- Validation mode: AI-manual (command-by-command), not runner-driven

## Version and Commit Under Test

- Installed package version: `codex-vl` = `codex-cli 0.140.0`
- Installed exec version: `codex-vl-exec` = `codex-exec 0.140.0`
- Repo branch under test: `develop`
- Commit under test: `f04dcec82`
- Git state after sync: clean (`develop...origin/develop`)

## Sanitized Environment Summary

- Platform: Android/Termux (`<platform-device>`)
- Test workspace: `<tmp-workspace>` created under the local user temp area
- Approval/sandbox observed in runtime smoke: `approval=never`, `sandbox=workspace-write` for the main smoke, with one TUI launch under `--dangerously-bypass-approvals-and-sandbox`
- Note: inline PTY/TUI capture on Termux is noisy and can emit terminal escape sequences instead of stable plain text

## Repo And Release State

Manual checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Observed:

- Branch tracks `origin/develop` and is clean.
- `HEAD` under test = `f04dcec82`.
- Dist-tags show:
  - `stable=0.135.0`
  - `latest=0.139.0`
  - `next=0.140.0`
  - platform `-next` tags for `linux-x64`, `linux-arm64`, `android-arm64`, and `darwin-arm64`
- Installed local binaries report `0.140.0`, matching the `next` release line.

Result: PASS

## Command Surface Results

Manual checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl debug prompt-input --help`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`

Observed:

- `codex-vl` and `codex-vl-exec` report aligned versions (`0.140.0`).
- Help routing works for the major subcommands listed above.
- `login status` is readable (`Logged in using ChatGPT`).
- MCP list prints configured servers with secret values redacted.
- Feature list is available and readable.

Result: PASS

## AI Tool Surface Results

Tool categories visible in-session during validation:

- Shell command execution
- Stdin/session continuation
- Patch application
- Plan updates

MCP visibility confirmed via CLI (`codex-vl mcp list`) for:

- `email_local`
- `memory`
- `sequential_thinking`
- `voice`
- `webfetch`

Result: PASS

## Installed Runtime Smoke Results

All smoke checks ran manually in `<tmp-workspace>` with the installed package.

1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed: correct working directory and `seed.txt` listed; no writes performed.
- Result: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- Observed: the tool created `hello.txt` and returned:
  - `seed`
  - `hello-codex-vl`
- Result: PASS

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed first status line: `HTTP/2 200`
- Result: PASS

## Manual TUI Checks

Interactive launch executed with:

- `codex-vl --no-alt-screen --dangerously-bypass-approvals-and-sandbox`

Observed:

- First launch rendered the startup surface with version `v0.140.0`, current workspace directory, and permission mode `YOLO mode`.
- The TUI booted MCP servers and reached a ready state.
- The visible Vivling startup surface rendered without crashing.
- Slash-command interaction was attempted in-session, but inline PTY capture on this device remained too noisy to preserve a clean plain-text transcript for `/loop`, `/goal`, `/vivling`, and `/vl`.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke in a temporary workspace.
- Runtime/write surface: verified in `workspace-write` mode by creating and reading back `hello.txt`.
- Patch surface: this report file was created as the patch artifact for the validation cycle.

Post-checks:

- `git diff --check`
- `git status --short`

Result: PASS

## Failures, Blockers, Residual Risk

- No core command or runtime failures.
- Environmental limitation: inline PTY/TUI capture on Termux is not deterministic enough to preserve clean slash-command transcripts in this session.
- Residual risk: slash-command behavior should be re-verified once on a terminal capture path that preserves stable plain text.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION
