# Codex-VL AI-Guided Surface Report (Linux)

- Date: 2026-06-04
- Platform: Linux (x86_64)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.137.0`
- Commit under test: `d5e48fa324`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and fast-forwarded to `origin/develop`.
- HEAD under test is `d5e48fa324` (`docs(test-report): add macOS AI-guided report for 0.137.0`).
- Installed binaries report `0.137.0`.
- `npm dist-tags` shows `latest: 0.136.2` and `next: 0.137.0`, with platform-specific `*-next` tags on the same release line.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from the installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.137.0`
- `codex-vl-exec --version` -> `codex-exec 0.137.0`
- Major help routes resolved successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned `Logged in using ChatGPT` without exposing credentials.
- `codex-vl mcp list` showed configured servers with sensitive values redacted.
- `codex-vl features list` rendered the expected fork- and goal-related feature inventory.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- interactive TUI/session control

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temporary workspaces:

### 4.1 Non-interactive exec

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`

Observed result:

- `OK`

Verdict: PASS.

### 4.2 `workspace-write` sandbox

- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`

Observed result:

- The run hit the host-side sandbox restriction:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`

Verdict: FAIL for this sandbox mode, environmental limitation only.

### 4.3 `danger-full-access` fallback

- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Observed results:

- Current directory and file listing returned the expected temporary workspace contents, including `seed.txt`.
- Write/readback returned:
  - `seed`
  - `hello-codex-vl`
- Network smoke returned:
  - `HTTP/2 200`

Verdict: PASS.

## 5. Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version `v0.137.0`, the working directory, and permission mode `YOLO mode`.
- The session reached the `ready` Vivling chat surface.
- `/loop ls` and `/goal` were attempted, but the PTY transcript is ANSI-heavy and not fully readable as plain text in this capture mode.
- The session exited cleanly on interrupt.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via `pwd` and `ls` in a temporary workspace.
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback under `danger-full-access`.
- Patch surface: this report file was added via the patch workflow and is reviewable as a normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- `workspace-write` runtime smoke is blocked by the host sandbox restriction (`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`).
- TUI slash-command transcript capture is noisy in this terminal, so the `/loop` and `/goal` checks are only partially observable from plain text.
- No core command-surface or installed-runtime regression was found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed `0.137.0` build passed the command surface, the non-interactive exec path, the `danger-full-access` runtime smoke, and the report patch workflow. The remaining failures are host/PTY limitations, not product regressions.
