# Codex-VL AI-Guided Surface Validation (Termux)

- Date: 2026-06-03
- Platform: Android/Termux (`<platform-device>`)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` 0.136.0
- Commit under test: `05b5ccb98`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and tracks `origin/develop`.
- HEAD is `05b5ccb98` (`docs(test-report): add macOS AI-guided report for 0.136.0`).
- Installed binaries report `0.136.0`.
- npm dist-tags show `latest: 0.135.0`, `next: 0.136.1`, and platform-specific next tags including `android-arm64-next: 0.136.1-android-arm64`.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.136.0`
- `codex-vl-exec --version` -> `codex-exec 0.136.0`
- Major help routes returned successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned a readable logged-in state without exposing secrets.
- `codex-vl mcp list` displayed configured servers with sensitive values redacted.
- `codex-vl features list` rendered the expected feature inventory, including `realtime_conversation` in under-development state.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories visible in-session during validation:

- shell command execution
- stdin/session continuation
- patch application
- plan management
- interactive TUI/session control

MCP visibility confirmed via CLI (`codex-vl mcp list`) for:

- `email_local`
- `memory`
- `sequential_thinking`
- `voice`
- `webfetch`

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in a disposable temp workspace under `$HOME`:

1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed output showed the temp workspace path and `seed.txt`.
- Result: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed first status line: `HTTP/2 200`
- Result: PASS

Verdict: PASS.

## 5. Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First-launch surface rendered with version `v0.136.0`, model line, working directory, and permission mode (`YOLO mode`).
- The startup trust prompt rendered correctly for the disposable temp workspace.
- The session shut down cleanly on interrupt.

Limitations:

- The Termux PTY capture is noisy for full-screen TUI interaction, so slash-command transcript evidence is not fully readable in plain text.
- I did not force a synthetic PASS for `/loop`, `/goal`, `/vivling`, or `/vl` beyond the observed startup and clean shutdown path.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via the temp-workspace runtime smoke above.
- Runtime/write surface: verified via `codex-vl-exec` file readback in the temp workspace.
- Patch surface: this report file was created via the patch workflow and then reviewed as a normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- Network smoke passed on this run, so no environmental blocker was observed there.
- TUI transcript capture remains noisy in this Termux PTY, so the slash-command subchecks are only partially observable in plain text.
- No core command-surface or installed-runtime regression found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: core CLI/runtime surfaces and read/write behavior passed on Termux for `0.136.0`; the remaining limitation is TUI transcript observability in this PTY, not a binary failure.
