# codex-vl 0.130.0 AI-Guided Surface Report

Date: 2026-05-09
Package: `@mmmbuto/codex-vl`
Version under test: `0.130.0`
Commit under test: `90d009bcec`
Branch: `develop`
Remote target for this phase: `origin/develop`

## Validation Method

Result: PASS WITH ENVIRONMENTAL LIMITATIONS

This report was produced by manual AI validation. The validating AI ran the
checks one command or interaction at a time, inspected the visible output, and
classified each surface directly.

No shell runner script, bulk `sh` test pipeline, TSV summary, or aggregate
runner exit code was used as the source of truth for this report. No Rust build
or `cargo test` result is part of this post-install verdict.

## Sanitized Environment

- Platform class: Linux `x64`
- Installed CLI: `codex-vl`
- Installed exec CLI: `codex-vl-exec`
- Temporary workspace: `<tmp-workspace>`
- Repository path: `<repo>`
- Secrets, absolute local paths, private service addresses, and raw environment
  dumps are intentionally omitted.

## Release State

PASS

- `codex-vl --version` reported `codex-cli 0.130.0`.
- `codex-vl-exec --version` reported `codex-exec 0.130.0`.
- npm dist-tags for `@mmmbuto/codex-vl` showed:
  - `latest`: `0.130.0`
  - `linux-x64`: `0.130.0-linux-x64`
  - `android-arm64`: `0.130.0-android-arm64`
  - `darwin-arm64`: `0.130.0-darwin-arm64`
- npm also printed non-blocking project config warnings for workspace-related
  settings. These did not affect package version resolution.

## Command Surface

PASS

Manual help/status checks succeeded for:

- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl debug prompt-input --help`
- `codex-vl completion bash`

`codex-vl login status` reported an authenticated ChatGPT login.

## MCP And Feature Surface

PASS

`codex-vl mcp list` succeeded and exposed the configured local MCP entries:

- `email_local`
- `memory`
- `nextcloud`
- `sequential_thinking`
- `voice`
- `webfetch`

`codex-vl features list` succeeded. Important observations:

- `goals` is present and stable.
- `shell_tool` is present and stable.
- `unified_exec` is present and stable.
- `tool_search` is present and stable.
- `sqlite` is marked removed.
- `tui_app_server` is marked removed.

## Runtime Smoke

PASS WITH ENVIRONMENTAL LIMITATION

Manual exact-output smoke:

- Command class: `codex-vl exec --skip-git-repo-check --ephemeral`
- Prompt: reply exactly `OK`
- Observed final answer: `OK`
- Result: PASS

Manual sandboxed exec smoke:

- Command class: `codex-vl-exec --sandbox workspace-write --json`
- Observed failure before the model could inspect files:
  `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- Result: ENVIRONMENTAL LIMITATION

The `bwrap` error means Bubblewrap could not set up the requested Linux sandbox
network namespace in this environment. It is not evidence of a failed CLI
install, but it blocks this specific sandbox mode on this host configuration.

Manual no-internal-sandbox read/list fallback:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox --json`
- Model override: `gpt-5.5`
- Observed behavior: command execution listed `seed.txt` and printed
  `<tmp-workspace>` as the current directory.
- Result: PASS

Manual no-internal-sandbox write/read:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox --json`
- Model override: `gpt-5.5`
- Observed behavior: created `hello.txt`, read `seed.txt` and `hello.txt`, and
  returned `seed` plus `hello-codex-vl`.
- Direct file check confirmed `hello-codex-vl`.
- Result: PASS

Manual network smoke:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox --json`
- Model override: `gpt-5.5`
- Observed final answer: `HTTP/2 200`
- Result: PASS

Default model note:

- A write/read attempt without model override failed because the configured
  default model `kimi-k2.6:cloud` is not supported with the active ChatGPT
  account.
- Result: CONFIGURATION RISK, not a filesystem runtime failure.

## Manual TUI Surface

PASS WITH ONE UNCLEAN CLEANUP CHECK

Manual TUI launch:

- Command class: `codex-vl --no-alt-screen --dangerously-bypass-approvals-and-sandbox`
- Observed header: `OpenAI Codex (v0.130.0)`
- Observed mode: YOLO permissions in `<tmp-workspace>`
- Result: PASS

Vivling surface:

- Observed pane: `Vivling chat - 0 messages - ready`
- Result: PASS

Loop slash surface:

- `/loop` displayed usage for add/list/show/on/off/remove/owner.
- `/loop ls` displayed no configured loops for the test thread.
- Result: PASS

Goal slash surface:

- `/goal` displayed usage and reported no current goal.
- `/goal test-report manual smoke` activated a goal and showed active goal
  status.
- Result: PASS

Goal cleanup:

- `/goal clear` was not cleanly verified in this run. While a goal was active,
  the input path fell into normal model execution and hit the unsupported
  default model error described above.
- The spawned TUI test process was interrupted and terminated after recording
  the behavior.
- Result: INCONCLUSIVE CLEANUP CHECK

## Read, Write, And Patch Behavior

PASS

- Runtime read/list and write/read were verified manually in `<tmp-workspace>`.
- Report edits were made as reviewable git changes under `test-report/`.
- The valid report flow did not run Rust tests or rebuild the project.

## Release Gate Decision

The installed `0.130.0` package is acceptable for the current `origin/develop`
test-report phase with these explicit limitations:

- Linux Bubblewrap sandbox setup fails in this host environment with
  `Failed RTM_NEWADDR: Operation not permitted`.
- The default configured model is incompatible with the active ChatGPT account;
  `gpt-5.5` override was required for runtime write/read and network smoke.
- `/goal clear` needs a clean follow-up check under a supported default model or
  explicit model override; `/goal` activation itself was observed working.

No GitHub push or GitHub release action is part of this report.
