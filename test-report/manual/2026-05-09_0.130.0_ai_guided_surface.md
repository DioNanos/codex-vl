# codex-vl 0.130.0 Termux AI-Guided Surface Report

Date: 2026-05-09
Package: `@mmmbuto/codex-vl`
Version under test: `0.130.0`
Branch under test: `develop`
Remote target for this phase: `origin/develop`
Source HEAD under test: `513085937`
Release merge commit: `90d009bce`
Platform class: Android/Termux `arm64`

## Validation Method

Result: PASS WITH TERMINAL LIMITATION

This report was produced by manual AI validation on Termux. The validating AI
ran the checks directly, inspected the visible output, and classified each
surface. No shell runner script, TSV summary, aggregate test script, Rust build,
or `cargo test` result was used as the source of truth for this post-install
report.

## Sanitized Environment

- Platform: `<platform-device>` Android/Termux `arm64`
- Installed CLI: `codex-vl`
- Installed exec CLI: `codex-vl-exec`
- Temporary workspace: `<tmp-workspace>`
- Repository path: `<repo>`
- Local usernames, absolute paths, private remotes, raw environment dumps, and
  unrelated process details are intentionally omitted.

## Release State

PASS

- `git fetch origin --prune` updated `origin/develop` from `90d009bce` to
  `513085937`.
- Local branch `develop` tracks `origin/develop` and was already fast-forwarded
  after checkout.
- Current source HEAD during validation: `513085937 test-report: document manual
  0.130.0 validation`.
- Release merge under validation remains `90d009bce Merge upstream
  rust-v0.130.0 for codex-vl 0.130.0`.
- npm dist-tags for `@mmmbuto/codex-vl` showed:
  - `latest`: `0.130.0`
  - `linux-x64`: `0.130.0-linux-x64`
  - `android-arm64`: `0.130.0-android-arm64`
  - `darwin-arm64`: `0.130.0-darwin-arm64`
- npm printed non-blocking workspace config warnings for pnpm-oriented settings.

## Command Surface

PASS

- `codex-vl --version` reported `codex-cli 0.130.0`.
- `codex-vl-exec --version` reported `codex-exec 0.130.0`.
- `codex-vl --help` rendered the expected command surface, including `exec`,
  `review`, `login`, `logout`, `mcp`, `plugin`, `app-server`, `remote-control`,
  `completion`, `sandbox`, `debug`, `resume`, `fork`, `cloud`, `exec-server`,
  and `features`.
- Focused help output succeeded for `exec`, `review`, `login`, `logout`,
  `resume`, `fork`, `mcp`, `sandbox`, `app-server generate-json-schema`, and
  `debug prompt-input`.
- `codex-vl completion bash` generated completion output successfully.
- `/tmp` was not writable in this Termux session; command-surface scratch output
  was written under the Termux temporary directory instead. This is an
  environment detail, not a CLI failure.
- `codex-vl login status` reported an authenticated ChatGPT login without
  exposing credentials.

## MCP And Feature Surface

PASS

`codex-vl mcp list` succeeded with secrets redacted and showed the configured
local Termux MCP entries enabled:

- `email_local`
- `memory`
- `sequential_thinking`
- `voice`
- `webfetch`

`codex-vl features list` succeeded. Relevant observations:

- `goals` is stable and enabled.
- `shell_tool` is stable and enabled.
- `unified_exec` is stable and enabled.
- `tool_search` is stable and enabled.
- `apps`, `plugins`, `hooks`, `multi_agent`, and `browser_use` are enabled.
- `sqlite` is marked removed.
- `tui_app_server` is marked removed.

## AI Tool Surface

PASS

The validating AI session had usable tool categories for:

- shell command execution;
- stdin/session continuation for long-running commands;
- patch/file editing through the agent workflow;
- loop management tools in the Codex session;
- MCP Memory category reads through the exposed `memory_read` tool.

No private MCP state files, JSON stores, SQLite files, or caches were read as a
source of truth. Memory bootstrap used the live MCP tool surface.

## Runtime Smoke

PASS

Manual exact-output smoke:

- Command class: `codex-vl exec --skip-git-repo-check --ephemeral`
- Prompt: reply exactly `OK`
- Observed final answer: `OK`
- Result: PASS

Manual read/list smoke:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox
  --skip-git-repo-check --json`
- Observed behavior: the model ran `pwd`, listed the temporary workspace, and
  found `seed.txt`.
- Result: PASS

Manual write/read smoke:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox
  --skip-git-repo-check --json`
- Observed behavior: the model created `hello.txt`, read `seed.txt` and
  `hello.txt`, and returned exactly:
  - `seed`
  - `hello-codex-vl`
- Direct file check confirmed `hello-codex-vl`.
- Result: PASS

Manual network smoke:

- Command class: `codex-vl-exec --dangerously-bypass-approvals-and-sandbox
  --skip-git-repo-check --json`
- Observed final answer: `HTTP/2 200`
- Result: PASS

The runtime smoke intentionally used no internal sandbox because this Android
Termux validation targets the installed package behavior on the phone. No Rust
build or Rust test command was run.

## Manual TUI Surface

PASS WITH TERMINAL LIMITATION

Manual TUI launch:

- Command class: `codex-vl --no-alt-screen
  --dangerously-bypass-approvals-and-sandbox`
- Observed startup warning: `TERM` was set to `dumb`; the test explicitly
  continued.
- Observed header: `OpenAI Codex (v0.130.0)`.
- Observed model line: `gpt-5.5 medium`.
- Observed permissions line: `YOLO mode`.
- Observed Vivling pane: `Vivling chat · 0 messages · ready`.
- Result: PASS

Slash surface:

- Typing `/loop` surfaced the inline description `manage local recurring prompts
  for this thread`.
- The TUI continued rendering after slash input and exited cleanly on interrupt,
  printing a resume command.
- Because the harness PTY reported `TERM=dumb` and emitted noisy control
  sequences, clean visual verification of `/loop ls`, `/goal`, `/goal clear`,
  `/vivling`, and `/vl` final panes is limited in this run.
- Result: PASS WITH TERMINAL LIMITATION

No loop jobs or persistent goal state were intentionally left active by this
validation run.

## Read, Write, And Patch Behavior

PASS

- Shell/read surface was verified in `<tmp-workspace>` with normal directory and
  file inspection.
- Runtime/write surface was verified by creating and reading `hello.txt` in
  `<tmp-workspace>`.
- Patch/report behavior is represented by this sanitized report update under
  `test-report/`.
- `git diff --check` is required before push and is not a Rust test.

## Failures, Blockers, And Residual Risk

- `TERM=dumb` limits high-fidelity interactive TUI capture in this harness.
  Startup, Vivling, and `/loop` discovery were visible; deeper slash-command pane
  verification should be repeated in a normal Termux terminal before public
  promotion if a maintainer requires full visual evidence.
- `/tmp` was not writable from this shell; Termux temporary storage worked.
- No Rust tests, Rust builds, release builds, or cargo commands were executed in
  this report phase by request.

## Release Gate Decision

PASS WITH TERMINAL LIMITATION

The installed `@mmmbuto/codex-vl@0.130.0` package passes the Termux
post-install command, MCP listing, feature listing, exec runtime, read/write,
network, and initial TUI/Vivling smoke checks for the `origin/develop` phase.

No GitHub push or GitHub release action is part of this report.
