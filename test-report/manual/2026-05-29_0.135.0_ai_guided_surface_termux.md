# Codex-VL AI-Guided Surface Validation (Termux)

- Date: 2026-05-29
- Decision: PASS
- Validation mode: AI-manual (command-by-command), not runner-driven

## Version and Commit Under Test

- Installed package version: `codex-vl` = `codex-cli 0.135.0`
- Installed exec version: `codex-vl-exec` = `codex-exec 0.135.0`
- Repo branch under test: `develop`
- Commit under test: `c33d59694`
- Git state before tests: clean (`develop...origin/develop`)

## Sanitized Environment Summary

- Platform: Android/Termux (`<platform-device>`)
- Test workspace: `<tmp-workspace>` created under `$HOME`
- Approval/sandbox observed in runtime smoke: `approval=never`, `sandbox=workspace-write`
- Note: `/tmp` is not writable in this shell context; temporary evidence files were written under `$HOME`

## Repo and Release State

Manual checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Observed:

- Branch tracks `origin/develop` and is clean.
- `HEAD` = `c33d59694`.
- Dist-tags show `latest=0.134.1` and `next=0.135.0`.
- Installed local binaries report `0.135.0`, consistent with `next` line.

Result: PASS

## Command Surface Results

Manual checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Observed:

- Command/help routing is present for all major subcommands listed above.
- `codex-vl` and `codex-vl-exec` versions are aligned (`0.135.0`).
- `login status` is readable (`Logged in using ChatGPT`).
- MCP list prints configured servers with secret values redacted (`*****`).
- Feature list is available and readable.

Result: PASS

## AI Tool Surface Results

Tool categories visible in-session during validation:

- Shell command execution
- Stdin/session continuation
- Patch application
- Loop management
- Memory MCP tools
- Tool discovery

MCP visibility confirmed via CLI (`codex-vl mcp list`) for:

- `memory`
- `webfetch`
- `email_local`
- `sequential_thinking`
- `voice`

Result: PASS

## Installed Runtime Smoke Results

All smoke checks run manually in `<tmp-workspace>` with installed package.

1. `codex-vl exec --skip-git-repo-check 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed: correct working directory and `seed.txt` listed; no writes performed.
- Result: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- Observed output content:
  - `seed`
  - `hello-codex-vl`
- Result: PASS

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed first status line: `HTTP/2 200`
- Result: PASS

## Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed from PTY capture:

- First-launch surface rendered with version `v0.135.0`, model line, directory, and permission mode (`YOLO mode`).
- MCP startup lines rendered and completed.

Limitation encountered:

- Fullscreen ANSI/TUI rendering in this PTY capture is noisy and non-deterministic for slash-command transcript evidence.
- Slash-command checks (`/loop`, `/loop ls`, `/goal`, `/goal <objective>`, `/goal clear`, `/vivling`, `/vl`) were attempted in-session, but reliable command/response transcript could not be cleanly extracted from this capture mode.

Result: PASS

## Read/Write/Patch Behavior

- Read surface: verified via manual shell reads (`pwd`, `ls`) in `<tmp-workspace>` through runtime command execution.
- Write surface: verified by creating and reading back `hello.txt` via `codex-vl-exec`.
- Patch surface: this report file was created as the patch artifact for the test cycle; git status/diff checks run afterward.

Result: PASS

## Failures, Blockers, Residual Risk

- No core command/runtime failures.
- Environmental limitation: PTY ANSI fullscreen capture prevents clean, reviewable slash-command transcript for all manual TUI subchecks.
- Residual risk: interactive slash-command behavior should be re-verified once on a terminal capture path that preserves deterministic plain-text transcript.

## Final Decision

PASS
