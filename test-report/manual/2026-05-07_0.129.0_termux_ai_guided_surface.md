# Codex-VL 0.129.0 Termux AI-Guided Surface Report

Date: 2026-05-07
Version under test: `0.129.0`
Branch under test: `develop`
Commit under test: `8475def15`
Suite type: AI-guided Termux runtime validation
Report hygiene: sanitized for public publication

## Summary

Decision: `PASS WITH OPERATOR-SCOPED LIMITATION`

The installed Termux runtime reports `codex-cli 0.129.0` and
`codex-exec 0.129.0`. Core command-surface checks passed, non-interactive exec
returned the expected exact response, workspace read/list and write/readback
passed, the network smoke returned `HTTP/2 200`, and first-launch TUI evidence
confirmed version, directory, YOLO mode, Vivling rendering, and `/loop` help.

The focused Rust test section from the reusable suite was not executed because
the operator explicitly requested no compile-based testing during this run. That
is an intentional scope limit for this report, not a hidden failure.

## Repo State

Observed state before report creation:

- branch: `develop`
- tracking: `origin/develop`
- HEAD: `8475def15`
- local untracked noise existed in `.tmp/` and was excluded from commit scope

The installed package under test matched the current release line in the repo.

## Sanitized Environment Summary

- platform: `<platform-device>` Android/Termux arm64
- Termux runtime string observed during the live session:
  `0.119.0-beta.3`
- package architecture observed: `aarch64`
- installed binaries under test:
  - `codex-vl`: `codex-cli 0.129.0`
  - `codex-vl-exec`: `codex-exec 0.129.0`

This report uses the exact runtime version string returned by the live Termux
session.

## Package And CLI Surface

Results:

- `PASS` `codex-vl --version`
- `PASS` `codex-vl-exec --version`
- `PASS` `codex-vl --help`
- `PASS` `codex-vl exec --help`
- `PASS` `codex-vl review --help`
- `PASS` `codex-vl login --help`
- `PASS` `codex-vl logout --help`
- `PASS` `codex-vl resume --help`
- `PASS` `codex-vl fork --help`
- `PASS` `codex-vl mcp --help`
- `PASS` `codex-vl sandbox --help`
- `PASS` `codex-vl app-server generate-json-schema --help`
- `PASS` `codex-vl completion bash`
- `PASS` `codex-vl login status`
- `PASS` `codex-vl mcp list`
- `PASS` `codex-vl features list`
- `PASS` `codex-vl debug prompt-input --help`

Observed login/MCP/features evidence:

- login status was readable without exposing credentials
- MCP list showed configured servers with secrets redacted
- feature listing included `goals`, `plugins`, `tool_search`, `unified_exec`,
  and related runtime surfaces relevant to this fork

## AI Tool Surface

The validating AI session had these usable tool categories:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- goal management
- live MCP memory read/context
- deferred tool discovery

Memory bootstrap was verified through live MCP Memory tools using the required
bootstrap categories. No direct Memory state files were used.

## Focused Rust Tests

Status: `SKIPPED BY OPERATOR REQUEST`

The reusable suite includes focused `cargo test` commands for fork-specific
logic. They were intentionally not part of this Termux validation because the
operator explicitly requested no compile-based testing during this run.

## Runtime Smoke

Runtime smoke was executed from `<tmp-workspace>`, not from the repo root.

Results:

- `PASS` `codex-vl exec --skip-git-repo-check --ephemeral "Reply with exactly: OK"`
- `PASS` workspace current-directory and file-list check
- `PASS` workspace write/readback check
- `PASS` network smoke

Observed evidence:

Exact response:

```text
OK
```

Read/write evidence:

```text
seed
hello-codex-vl
```

Network evidence:

```text
HTTP/2 200
```

Additional runtime note:

- during the write/readback smoke, an internal `apply_patch` attempt was
  rejected in `workspace-write` mode with a "writing outside of the project"
  message, but the runtime completed the task through an allowed shell-write
  fallback and correctly read both files back

## Manual TUI Checks

Manual TUI evidence was collected from live Termux sessions.

Results:

- `PASS` first launch rendered `OpenAI Codex (v0.129.0)`
- `PASS` working directory rendered
- `PASS` YOLO permission mode rendered
- `PASS` Vivling surface rendered
- `PASS` `/loop` help rendered
- `PARTIAL` deeper slash-command capture

Partial limitation:

- the raw TUI capture stream on this Termux session was noisy, so deeper
  assertions such as `/loop ls`, `/goal` lifecycle, and additional Vivling
  interaction are not claimed as full PASS evidence in this report

## Read, Write, And Patch Behavior

Results:

- `PASS` shell/read surface through normal command execution in a temporary
  workspace
- `PASS` runtime/write surface through `codex-vl-exec`
- `PASS` patch surface for report creation in the validating AI session

The report file itself was created as a reviewable git change intended for a
normal commit/push flow.

## Blockers And Residual Risk

Blockers:

- none for the operator-scoped runtime validation performed here

Residual risk:

- focused Rust tests were intentionally omitted in this run
- deeper TUI command lifecycle coverage should be re-run in a cleaner
  interactive capture path before making stronger claims about all interactive
  surfaces

## Final Decision

Decision: `PASS WITH OPERATOR-SCOPED LIMITATION`

For the scope actually requested and executed here, the installed Termux
`codex-vl` runtime is acceptable: command surface, runtime exec, workspace
read/write, network smoke, and first-launch TUI surface all behaved correctly.
The only limitations are the deliberate omission of compile-based focused Rust
tests and partial-depth TUI evidence due to noisy live capture.
