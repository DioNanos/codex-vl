# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-05-29
- Platform: macOS (Apple Silicon)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` 0.135.0
- Commit under test: `0299f4c4c3`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and clean before report write.
- HEAD is `0299f4c4c3` (`docs(readme): promote release channels to stable 0.135.0 latest`).
- Installed binaries report `0.135.0`.
- npm dist-tags returned `latest: 0.134.1` and `next: 0.135.0`.

Verdict: PASS (release-candidate binary/version checks are coherent for `0.135.0` next channel).

## 2. Package And Command Surface

Checks executed from installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.135.0`
- `codex-vl-exec --version` -> `codex-exec 0.135.0`
- All requested help routes returned successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned logged-in state without secret leakage.
- `codex-vl mcp list` displayed configured servers with sensitive env values redacted.
- Feature inventory rendered and included expected runtime capability families.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- MCP/app tool discovery and callable tool interfaces

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temp workspace:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt ... read seed.txt and hello.txt ...'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com ...'`

Evidence summary:

- Exact `OK` response received.
- Directory/read listing returned expected files.
- Write/read test succeeded (`seed` and `hello-codex-vl` returned).
- Network check failed with environment-level DNS resolution error (`Could not resolve host: www.google.com`), not a command-routing/runtime crash.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION (DNS in test environment).

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Evidence summary:

- First launch rendered startup surface (version, model, directory, permissions).
- Session showed trust/onboarding prompts and entered interactive screen.
- Slash-command submission was attempted for `/loop`, `/loop ls`, `/goal ...`, `/goal clear`, `/vivling`, `/vl`.
- Session terminated cleanly with `Shutting down...`.

Limitations:

- A first-run external-agent migration prompt interleaved with startup flow.
- TUI transcript capture in this terminal mode is ANSI-frame heavy; discrete per-command response lines are partially observable.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke (`pwd`, `ls`, file reads).
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback.
- Patch surface: this report file was created via patch workflow and staged as normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- Environmental DNS resolution failure during external network smoke.
- TUI first-run migration prompt can add nondeterministic startup steps on fresh/rotated local profiles.
- No core command-surface regression found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: core CLI/runtime surfaces and read/write behavior passed on macOS for `0.135.0`; observed issues were environmental (DNS) and first-run TUI prompt noise, not core binary failures.
