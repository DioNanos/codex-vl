# Codex-VL AI-Guided Surface Report (Linux)

- Date: 2026-06-03
- Platform: Linux (x86_64)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` 0.136.0
- Commit under test: `be1a6b701b`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and clean before report write.
- HEAD is `be1a6b701b` (`docs(test-report): add Termux AI-guided report for 0.136.0`).
- Installed binaries report `0.136.0`.
- npm dist-tags returned `latest: 0.135.0`, `next: 0.136.1`, and platform-specific next tags including `linux-x64-next`, `linux-arm64-next`, and `android-arm64-next`.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.136.0`
- `codex-vl-exec --version` -> `codex-exec 0.136.0`
- All requested help routes returned successfully.
- `codex-vl completion bash` generated output successfully.
- `codex-vl login status` returned `Logged in using ChatGPT` without exposing credentials.
- `codex-vl mcp list` displayed configured servers with sensitive values redacted.
- `codex-vl features list` rendered the expected feature inventory.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- interactive TUI/session control

MCP visibility confirmed via CLI (`codex-vl mcp list`) for:

- `email_local`
- `memory`
- `nextcloud`
- `sequential_thinking`
- `vlmsa`
- `voice`
- `webfetch`

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temp workspaces:

### 4.1 `workspace-write` sandbox

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Exact `OK` response received from the non-interactive exec path.
- The `workspace-write` shell and network checks failed with:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- The write test also failed at file creation time under the same host restriction.
- This is a host-environment limitation in the local sandbox wrapper, not a codex-vl binary regression.

### 4.2 `danger-full-access` fallback

- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Read/list check returned the temp workspace contents and `seed.txt`.
- Write/readback check returned:
  - `seed`
  - `hello-codex-vl`
- Network check returned:
  - `HTTP/2 200`

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version `v0.136.0`, model, working directory, and permission mode.
- The initial screen and progress surfaces rendered correctly in the PTY.
- Slash-command verification (`/loop`, `/goal`, `/vivling`, `/vl`) was attempted, but the terminal capture was ANSI-heavy and not fully readable as plain text in this environment.

Limitations:

- This PTY capture mode makes full-screen TUI interaction noisy.
- I did not force a synthetic PASS for the slash-command subchecks beyond the observable startup surface.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke in a temporary workspace.
- Runtime/write surface: verified under `danger-full-access`; the `workspace-write` path is blocked by the host `bwrap` loopback restriction.
- Patch surface: this report file was created via the patch workflow and then reviewed as a normal git diff.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- `workspace-write` runtime smoke is blocked by a host environment restriction (`bwrap` loopback RTM_NEWADDR not permitted).
- TUI slash-command evidence is partially obscured by terminal transcript noise.
- No core command-surface or installed-runtime regression was found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed package version and command surface are correct, the manual runtime read/write/network smoke passes under `danger-full-access`, and the remaining failures are host/sandbox limitations rather than product regressions.
