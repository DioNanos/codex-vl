# Codex-VL 0.128.4 AI-Guided Surface Report

Date: 2026-05-06
Version under test: `0.128.4`
Branch under test: `develop`
Commit under test: `09d2fd067c`
Suite type: AI-guided manual and runtime validation
Report hygiene: sanitized for public publication

## Summary

Decision: `PASS WITH ENVIRONMENTAL LIMITATION`

The installed `codex-vl` package reports `codex-cli 0.128.4`, the installed
`codex-vl-exec` reports `codex-exec 0.128.4`, the focused fork tests for
dynamic tools and goals pass, non-interactive exec works, workspace read/write
works, and manual TUI checks verified first launch, Vivling rendering, `/loop`
usage/list, `/goal` set, interrupt/pause, and clear.

The only runtime smoke failure was the network check: DNS resolution failed for
`www.google.com` in the test environment. This is recorded as an environmental
limitation because the command executed and returned a clear resolver error.

## Repo State

Observed state:

- branch: `develop`
- tracking: `origin/develop`
- HEAD: `09d2fd067c`
- local untracked payloads were present but unrelated to this report:
  `.artifacts/` and `vendor-src/`

No release binaries or private local paths are included in this report.

## Package And CLI Surface

Results:

- `PASS` `codex-vl --version`
- `PASS` `codex-vl-exec --version`
- `PASS` `codex-vl --help`
- `PASS` `codex-vl exec --help`
- `PASS` `codex-vl review --help`
- `PASS` `codex-vl login --help`
- `PASS` `codex-vl logout --help`
- `PASS` `codex-vl resume --help`
- `PASS` `codex-vl fork --help`
- `PASS` `codex-vl mcp --help`
- `PASS` `codex-vl sandbox --help`
- `PASS` `codex-vl completion bash`
- `PASS` `codex-vl app-server generate-json-schema --out <tmp-dir>`
- `PASS` `codex-vl login status`
- `PASS` `codex-vl mcp list`
- `PASS` `codex-vl features list`
- `PASS` `codex-vl debug prompt-input --help`

MCP listing was verified with secret values redacted by the CLI.

## AI Tool Surface

The validating AI session had these usable tool categories:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- MCP memory read/search through live MCP discovery
- deferred tool discovery

MCP memory was verified through live MCP reads for the required MGM bootstrap
categories. No direct memory state files were read.

Loop management was tested at the tool level by creating, showing, and removing
a temporary monitor loop.

## Focused Rust Tests

Commands run:

```sh
cargo test --manifest-path codex-rs/Cargo.toml -p codex-tools dynamic_tools -- --nocapture
cargo test --manifest-path codex-rs/Cargo.toml -p codex-tools goal_tools -- --nocapture
cargo test --manifest-path codex-rs/Cargo.toml -p codex-app-server dynamic_tools -- --nocapture
cargo test --manifest-path codex-rs/Cargo.toml -p codex-state goals -- --nocapture
```

Results:

- `PASS` `codex-tools dynamic_tools`: 3 passed
- `PASS` `codex-tools goal_tools`: 1 passed
- `PASS` `codex-app-server dynamic_tools`: 13 unit tests plus 8 suite tests passed
- `PASS` `codex-state goals`: 18 passed

Coverage confirmed:

- dynamic `manage_loops` registration
- app-server dynamic tool validation
- dynamic tool call round trip
- built-in dynamic tools returned on resume
- goal tool feature gating
- goal replacement, pause, clear/delete, budgeting, completion, and usage
  accounting

## Runtime Smoke

Runtime smoke was run from `<tmp-workspace>`, not from the repo root.

Results:

- `PASS` `codex-vl exec --skip-git-repo-check --ephemeral "Reply with exactly: OK"`
- `PASS` `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json` directory/listing check
- `PASS` workspace read/write/readback check
- `ENVIRONMENT FAIL` network smoke

Read/write evidence:

```text
seed
hello-codex-vl
```

Network failure:

```text
curl: (6) Could not resolve host: www.google.com
```

This confirms the runtime could execute the network command, but the local test
environment could not resolve DNS at that moment.

## Manual TUI Checks

Manual TUI checks were performed in a throwaway interactive session.

Results:

- `PASS` first launch rendered `OpenAI Codex v0.128.4`
- `PASS` model, working directory, and YOLO permission mode rendered
- `PASS` Vivling CRT/status rendered and animated
- `PASS` `/loop` usage rendered
- `PASS` `/loop ls` returned no configured loops
- `PASS` `/goal` returned usage and no-goal state
- `PASS` `/goal <objective>` created an active goal and updated status
- `PASS` interrupt paused the active goal
- `PASS` `/goal clear` cleared the goal
- `PARTIAL` deeper `/loop add/show/off/owner/rm` TUI lifecycle was not completed
  in the TUI because the disposable terminal session closed during follow-up
  input; the same loop lifecycle was verified through the live loop tool and by
  focused Rust tests

No intentional test loop or goal was left active.

## Patch Verification

Patch surface was exercised by adding this report and the reusable
AI-guided suite guide through the agent patch tool.

Expected follow-up checks before committing:

```sh
git diff --check
git status --short
```

## Comparison With Codex-Termux Runtime Pattern

This suite mirrors the practical `codex-termux` coverage pattern:

- version checks
- help routing checks
- login/MCP/features checks
- non-interactive exec smoke
- workspace read/write smoke
- network smoke
- binary/package surface checks where applicable

For `codex-vl`, the suite extends that pattern with:

- Vivling manual checks
- `/loop` manual and dynamic-tool checks
- `/goal` manual, tool, and state-accounting checks
- app-server dynamic tool schema/round-trip checks
- AI tool-surface inventory
- patch-tool verification

## Blockers And Residual Risk

Blockers:

- none for local 0.128.4 AI-guided validation

Residual risk:

- network smoke should be re-run in an environment with working DNS before a
  final public release claim
- full `/loop add/show/off/owner/rm` TUI lifecycle should be re-run manually in
  a stable interactive terminal before final promotion
- platform-specific macOS and Android device tests remain separate release
  acceptance gates
