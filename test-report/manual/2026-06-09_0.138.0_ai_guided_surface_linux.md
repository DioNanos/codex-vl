# Codex-VL AI-Guided Surface Report (Linux)

- Date: 2026-06-09
- Platform: Linux (x86_64)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.138.0`
- Commit under test: `32d4517867`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and tracks `origin/develop`.
- HEAD under test is `32d4517867` (`docs(test-report): add macOS AI-guided report for 0.138.0`).
- Installed binaries report `0.138.0`.
- `npm dist-tags` show `stable: 0.135.0`, `latest: 0.137.0`, and `next: 0.138.0`, with matching `*-next` platform tags.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from the installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.138.0`
- `codex-vl-exec --version` -> `codex-exec 0.138.0`
- Major help routes resolved successfully.
- `codex-vl completion bash` generated a completion file successfully.
- `codex-vl login status` returned a readable logged-in state without exposing credentials.
- `codex-vl mcp list` showed configured servers with secrets redacted.
- `codex-vl features list` exposed the expected feature inventory, including goal and unified-exec surfaces.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temporary workspaces:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Non-interactive exec returned `OK`.
- `workspace-write` hit the host sandbox blocker before the command could run:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- The same listing smoke under `danger-full-access` returned the expected temporary workspace and `seed.txt`.
- The write/read test succeeded and returned:
  - `seed`
  - `hello-codex-vl`
- The network smoke returned:
  - `HTTP/2 200`

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox --no-alt-screen`

Observed behavior:

- First launch rendered the startup surface with version `v0.138.0`, current workspace directory, and permission mode `YOLO mode`.
- MCP servers booted and the UI reached a stable `ready` state.
- The session exited cleanly on `Ctrl-D`.

Limitation:

- Slash-command surfaces such as `/loop`, `/goal`, `/vivling`, and `/vl` were not reliably machine-readable in this PTY capture because the terminal output was ANSI-heavy and animated. I am not claiming full slash-command verification from this capture alone.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via `pwd` and `ls` in a temporary workspace.
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback under `danger-full-access`.
- Patch surface: this report file was added via the patch workflow and is the only intended repo change.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- `workspace-write` runtime smoke is blocked by the host sandbox restriction (`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`).
- The TUI capture is noisy in this terminal, so interactive slash-command behavior is only partially observable from plain text.
- No core command-surface or installed-runtime regression was found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed `0.138.0` build passed the command surface, the non-interactive exec path, the `danger-full-access` runtime smoke, and the report patch workflow. The remaining failures are host/PTY limitations, not product regressions.
