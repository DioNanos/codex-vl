# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-06-04
- Platform: macOS (Apple Silicon)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.137.0`
- Commit under test: `6ddc534a60`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and now fast-forwarded to `origin/develop`.
- HEAD under test is `6ddc534a60` (`chore: drop stale .snap.new artifacts (tracked since old snapshot commit)`).
- Installed binaries report `0.137.0`.
- `npm dist-tags` and the repo version are aligned with the release candidate currently staged on `develop`.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from the installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.137.0`
- `codex-vl-exec --version` -> `codex-exec 0.137.0`
- Major help routes resolved successfully.
- `codex-vl completion bash` generated a completion script successfully.
- `codex-vl login status` returned a readable logged-in state without exposing secrets.
- `codex-vl mcp list` showed configured servers with sensitive values redacted.
- `codex-vl features list` included the expected feature inventory, including fork- and goal-related features.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- plan management

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in a disposable temporary workspace:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Exact `OK` response received from the non-interactive exec path.
- Directory/read listing returned the expected temporary workspace contents, including `seed.txt`.
- Write/read test succeeded with `seed` and `hello-codex-vl` returned in order.
- Network check returned `HTTP/2 200` from `curl -I https://www.google.com`.

Verdict: PASS.

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version `v0.137.0`, model selection/loading state, current workspace directory, and permission mode `YOLO mode`.
- The Vivling/chat surface rendered successfully and reached a ready state.
- `/loop ls` and `/goal` were attempted in the interactive session, but the plain-text transcript is ANSI-heavy and not cleanly observable in this terminal capture.
- The session was exited without leaving a crash or shell error.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via `pwd` and `ls` in a temporary workspace.
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback.
- Patch surface: this report file was added via the patch workflow and will be reviewed through git diff checks.

Post-checks planned and/or executed:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- Interactive TUI slash-command transcript capture is noisy in this terminal, so the `/loop` and `/goal` checks are only partially observable from plain text.
- No core command-surface or installed-runtime regression found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed `0.137.0` build passed the required command surface, runtime smoke, and report patch workflow on macOS; the only limitation was terminal transcript noise during the interactive TUI checks.
