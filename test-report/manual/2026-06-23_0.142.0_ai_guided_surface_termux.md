# Codex-VL AI-Guided Surface Validation (Termux)

- Date: 2026-06-23
- Decision: PASS WITH ENVIRONMENTAL LIMITATION
- Validation mode: AI-manual (command-by-command), not runner-driven

## Version and Commit Under Test

- Installed package version: `codex-vl` = `codex-cli 0.142.0`
- Installed exec version: `codex-vl-exec` = `codex-exec 0.142.0`
- Repo branch under test: `develop`
- Commit under test: `726438100`
- Git state after sync: clean (`develop...origin/develop`)

## Sanitized Environment Summary

- Platform: Android/Termux (`<platform-device>`)
- Termux app version observed: `0.118.0-ai.14`
- CPU architecture: `aarch64`
- Test workspace: `<tmp-workspace>` created under the Termux temp area
- Approval/sandbox observed in runtime smoke: `approval=never`, `sandbox=workspace-write`
- Interactive TUI check used `--no-alt-screen --dangerously-bypass-approvals-and-sandbox`

## Repo And Release State

Manual checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Observed:

- Branch tracks `origin/develop` and is clean after sync.
- `HEAD` under test = `726438100` (`chore: bump @mmmbuto/codex-vl to 0.142.0`).
- Dist-tags show:
  - `stable=0.140.0`
  - `latest=0.140.0`
  - `next=0.142.0`
  - platform tags for `android-arm64`, `linux-x64`, `linux-arm64`, and `darwin-arm64` at `0.142.0`
- Installed local binaries report `0.142.0`, matching the `next` release line.

Result: PASS

## Command Surface Results

Manual checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl debug prompt-input --help`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl completion bash > <tmp-completion>`

Observed:

- `codex-vl` and `codex-vl-exec` report aligned versions (`0.142.0`).
- Help routing works for the major subcommands listed above.
- `login status` is readable (`Logged in using ChatGPT`).
- MCP list prints configured servers with secrets redacted and shows `memory`, `webfetch`, `email_local`, `voice`, and `sequential_thinking`.
- Feature list is readable and completion generation succeeds.

Result: PASS

## AI Tool Surface Results

Tool categories visible to the validating AI in-session:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- goal management

MCP status during validation:

- MCP tools were not exposed directly to this AI session.
- Memory bootstrap was attempted via direct stdio fallback against `mcp-memory-rs`, per device policy.
- `tools/list` succeeded on the memory server, but the requested bootstrap categories were absent locally.
- CLI-side MCP visibility was confirmed through `codex-vl mcp list`.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Installed Runtime Smoke Results

All smoke checks ran manually in `<tmp-workspace>` with the installed package.

1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed: correct working directory and `seed.txt` listed; no writes performed.
- Result: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- Observed: the tool first attempted `apply_patch`, which was rejected in the non-repo temp workspace as "writing outside of the project".
- Observed fallback: the agent used a direct shell write inside the workspace, then returned:
  - `seed`
  - `hello-codex-vl`
- Result: PASS WITH ENVIRONMENTAL LIMITATION

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed first status line: `HTTP/2 200`
- Result: PASS

## Manual TUI Checks

Interactive launch executed with:

- `codex-vl --no-alt-screen --dangerously-bypass-approvals-and-sandbox`

Observed:

- First launch rendered the startup surface with version `v0.142.0`, active model `gpt-5.4`, current workspace directory, and permission mode `YOLO mode`.
- The visible Vivling startup surface rendered without crashing and reached a ready state.
- Slash-command capture on the inline Termux PTY remained noisy and buffered. Early slash commands could concatenate if sent before the UI was fully settled.
- Despite the buffering noise, the session showed:
  - slash-command parsing and feedback (`Unrecognized command '/looploop'`)
  - goal activation/progress output
  - `Ctrl-C` pausing the active goal instead of crashing the TUI
- Resume/fork help is present from the command surface checks.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke in a temporary workspace.
- Runtime/write surface: verified in `workspace-write` mode by creating and reading back `hello.txt`.
- Patch surface: this report file was created as the patch artifact for the validation cycle.

Post-checks required after report creation:

- `git diff --check`
- `git status --short`

Result: PASS

## Failures, Blockers, Residual Risk

- No core command-surface or runtime-read failures.
- Environmental limitation: direct MCP tool exposure was absent in the validating AI session, so Memory bootstrap used the allowed stdio fallback and the CLI was used to confirm configured MCP servers.
- Environmental limitation: inline Termux PTY capture is not deterministic enough to preserve clean slash-command transcripts; buffered input can concatenate early commands if the UI has not fully settled.
- Environmental limitation: `apply_patch` inside `codex-vl-exec` treated the disposable temp workspace as outside the project boundary, so the agent fell back to a direct shell write for the smoke test.
- Residual risk: slash-command ergonomics on Termux PTY should be re-verified once in a cleaner terminal capture path before wider release promotion.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION
