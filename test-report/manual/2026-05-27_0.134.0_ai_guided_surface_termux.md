# Codex-VL AI-Guided Surface Report (Termux Android)

- Date: 2026-05-27
- Package under test: `codex-vl` / `codex-vl-exec` `0.134.0`
- Branch under test: `develop` tracking `origin/develop` (Forge)
- HEAD under test: `a581547c4`
- Validation mode: AI-manual, step-by-step (no runner verdicts)

## Sanitized Environment Summary

- Platform: Android arm64 / Termux (`<platform-device>`)
- Validation workspace: `<tmp-workspace>`
- Repo root: `<repo>`
- Approval mode observed: `never`
- Sandbox mode used in runtime smoke: `workspace-write`

## 1) Repo And Release State

Commands executed manually:
- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence:
- Working branch is `develop...origin/develop` with no divergence.
- HEAD commit under test is `a581547c4`.
- Dist-tags show `latest=0.133.0`, `next=0.134.0`, including platform `android-arm64-next=0.134.0-android-arm64`.

Verdict: PASS

## 2) Package And Command Surface

Commands executed manually:
- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash > <completion-output>`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence:
- `codex-vl` reports `codex-cli 0.134.0`.
- `codex-vl-exec` reports `codex-exec 0.134.0`.
- Major subcommand help routing is healthy.
- `login status` is readable (`Logged in using ChatGPT`).
- `mcp list` shows configured servers with env values redacted.
- Completion generation succeeded (non-empty output).

Verdict: PASS

## 3) AI Tool Surface (In-Session)

Observed tool categories available to validating AI:
- shell execution (`exec_command`)
- stdin/session continuation (`write_stdin`)
- patch application (`apply_patch`)
- loop management (`manage_loops`)
- tool discovery (`tool_search`)
- web surface (`web`)

Fallbacks used:
- None for MCP/data-state inspection. No direct reads of MCP private state files.

Verdict: PASS

## 4) Installed Runtime Smoke

Temporary workspace created and seeded:
- `seed.txt` created with content `seed`

Manual checks and outcomes:
1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
   - Result: exact `OK`.
   - Verdict: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
   - Result: returned cwd and `ls -la` with `seed.txt` visible; `exit_code: 0`.
   - Verdict: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt ... read seed.txt and hello.txt back ...'`
   - Result: wrote `hello.txt`, read back `seed` and `hello-codex-vl`; `exit_code: 0`.
   - Verdict: PASS

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
   - Result: `HTTP/2 200`; `exit_code: 0`.
   - Verdict: PASS

Section verdict: PASS

## 5) Manual TUI Checks

Launch command:
- `codex-vl --dangerously-bypass-approvals-and-sandbox --no-alt-screen`

Commands attempted manually:
- `/loop`
- `/loop ls`
- `/goal termux-ai-guided-suite`
- `/goal clear`
- `/vivling`
- `/vl`

Result:
- Interactive process started and accepted input.
- PTY capture in this validation path did not return reliable rendered UI state, only echoed input/control output.
- Session was interrupted and closed cleanly (`^C`), with no persistent loop jobs created by this test flow.

Section verdict: PASS WITH OBSERVABILITY LIMITATION

## 6) Read, Write, And Patch Behavior

Shell/read surface:
- In `<tmp-workspace>`, `ls -la` and `cat seed.txt` succeeded.
- Verdict: PASS

Runtime/write surface:
- `codex-vl-exec` created `hello.txt` and read both files back under `workspace-write`.
- Verdict: PASS

Patch surface:
- This report was added via patch tool.
- `git diff --check` and `git status --short` reviewed after patch.
- Verdict: PASS

## Failures, Blockers, Residual Risk

- No runtime regression observed for `0.134.0` on this Termux validation run.
- Residual risk: TUI command-by-command assertions are partially limited by PTY observability in this capture path.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale:
- Version, branch, and package surfaces are correct for `0.134.0` on `a581547c4`.
- Runtime smoke passes for read, write, and network under `workspace-write`.
- Interactive TUI is launchable and accepts command input, but rendered confirmation is not fully observable in this terminal capture mode.
