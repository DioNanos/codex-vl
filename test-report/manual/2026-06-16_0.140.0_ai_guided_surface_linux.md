# Codex-VL AI-Guided Surface Report (Linux)

- Date: 2026-06-16
- Platform: Linux (x86_64)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.140.0`
- Commit under test: `a583726a10`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- The checkout used for validation was a local fork working copy.
- `origin` points to the fork's private work remote.
- `develop` fast-forwarded cleanly to `a583726a10` during this run.
- Installed binaries reported `0.140.0`.
- npm dist-tags reported:
  - `latest: 0.139.0`
  - `next: 0.140.0`
  - platform next tags for `linux-x64`, `linux-arm64`, `android-arm64`, and `darwin-arm64`
- The worktree contained pre-existing untracked snapshot artifacts under `codex-rs/tui/src/.../*.snap.new`; I left them untouched because they are unrelated to this validation report.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.140.0`
- `codex-vl-exec --version` -> `codex-exec 0.140.0`
- Help routing resolved successfully for the main command surface and the requested subcommands.
- `codex-vl login status` returned `Logged in using ChatGPT` without exposing credentials.
- `codex-vl mcp list` showed configured MCP servers with secrets redacted.
- `codex-vl features list` exposed the expected feature inventory, including the fork-relevant surfaces.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temporary workspaces:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox danger-full-access --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- The non-interactive exec path returned `OK`.
- The `workspace-write` smoke hit the host sandbox blocker before command execution:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- The same listing smoke under `danger-full-access` returned the expected temporary workspace and `seed.txt`.
- The write/read smoke succeeded and returned:
  - `seed`
  - `hello-codex-vl`
- The network smoke returned:
  - `HTTP/2 200`

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version `v0.140.0`, current workspace directory, and permission mode `YOLO mode`.
- The TUI booted MCP servers and rendered the standard startup surface correctly.
- The TUI showed the expected live workspace and readiness banner.

Limitation:

- I attempted slash-command interaction (`/loop`, `/goal test manual`) in the live TUI session.
- The PTY capture was ANSI-heavy and animated, so the slash-command subchecks were not machine-readable enough to claim a full plain-text PASS for `/loop`, `/goal`, `/vivling`, and `/vl` from this transcript alone.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via runtime smoke in a temporary workspace.
- Runtime/write surface: verified under `danger-full-access`; the `workspace-write` path is blocked by the host `bwrap` loopback restriction.
- Patch surface: this report file was created via the patch workflow and is the intended repo change.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- `workspace-write` runtime smoke is blocked by a host environment restriction (`bwrap` loopback RTM_NEWADDR not permitted).
- TUI slash-command evidence is partially obscured by terminal transcript noise.
- No core command-surface or installed-runtime regression was found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed package version and command surface are correct, the manual runtime read/write/network smoke passes under `danger-full-access`, and the remaining failures are host/sandbox limitations rather than product regressions.
