# Codex-VL AI-Guided Surface Validation (Linux)

- Date: 2026-06-23
- Platform: Linux (x86_64)
- Decision: PASS WITH ENVIRONMENTAL LIMITATION
- Validation mode: AI-manual (command-by-command), not runner-driven

## Version and Commit Under Test

- Installed package version: `codex-vl` = `codex-cli 0.142.0`
- Installed exec version: `codex-vl-exec` = `codex-exec 0.142.0`
- Repo branch under test: `develop`
- Commit under test: `afcddaf1f4`
- Git state after sync: clean (`develop...origin/develop`)

## Sanitized Environment Summary

- Platform: Linux (`x86_64`)
- Test workspace: `<tmp-workspace>`
- Approval/sandbox observed in non-interactive exec: `approval=never`
- TUI check used `--no-alt-screen --dangerously-bypass-approvals-and-sandbox`
- Report was written from the validating AI session using the patch tool.

## Repo And Release State

Manual checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Observed:

- Branch tracks `origin/develop` and was clean after sync.
- `HEAD` under test = `afcddaf1f4` (`docs(test-report): add termux AI-guided report for 0.142.0`).
- Release-line commit present immediately below HEAD = `7264381008` (`chore: bump @mmmbuto/codex-vl to 0.142.0`).
- Dist-tags show:
  - `stable=0.140.0`
  - `latest=0.140.0`
  - `next=0.142.0`
  - platform tags for `linux-x64`, `linux-arm64`, `android-arm64`, and `darwin-arm64` at `0.142.0`
- Installed local binaries report `0.142.0`, matching the `next` release line and Linux platform tag.

Result: PASS

## Command Surface Results

Manual checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl debug prompt-input --help`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl completion bash > <tmp-completion>`

Observed:

- `codex-vl` and `codex-vl-exec` report aligned versions (`0.142.0`).
- Help routing works for the major subcommands listed above.
- Completion generation succeeded.
- `login status` is readable (`Logged in using ChatGPT`).
- `mcp list` prints configured servers with secrets redacted.
- Configured MCP names visible from the installed CLI include `email_local`, `memory`, `nextcloud`, `sequential_thinking`, `vlmsa`, `voice`, `webfetch`, and `github`.
- Feature list is readable and includes stable goal, shell, unified exec, memories, apps, plugin, and tool surfaces.

Result: PASS

## AI Tool Surface Results

Tool categories visible to the validating AI in-session:

- shell command execution
- stdin/session continuation
- patch application
- loop management
- goal management
- MCP/resource discovery through the host session

Manual AI-level tool checks:

- `get_goal` reported no active goal before the smoke.
- `create_goal` successfully created a test goal.
- `update_goal(status=complete)` completed that test goal; the goal tool reported 617 tokens and about 4 seconds of elapsed test time.
- `manage_loops list` initially reported no jobs.
- `manage_loops add` successfully created `codex-vl-linux-report-smoke`.
- `manage_loops list` showed the created loop job.
- `manage_loops remove` removed it.
- Final `manage_loops list` returned an empty job list.

Observed limitation:

- The loop add call accepted the test job but created it enabled even though the request included `enabled=false`; the job was removed immediately and no loop jobs were left behind.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Installed Runtime Smoke Results

All smoke checks ran manually in `<tmp-workspace>` with the installed package.

1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed failure before shell execution:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- Result: ENVIRONMENTAL LIMITATION

3. Same listing smoke with `--sandbox danger-full-access`
- Observed: correct working directory and `seed.txt` listed; no writes performed.
- Result: PASS

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- Observed: file-change and shell fallback both failed under the same local `bwrap` loopback restriction.
- Result: ENVIRONMENTAL LIMITATION

5. Same write/read smoke with `--sandbox danger-full-access`
- Observed response:
  - `seed`
  - `hello-codex-vl`
- Result: PASS

6. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed failure before `curl` execution:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
- Result: ENVIRONMENTAL LIMITATION

7. Same network smoke with `--sandbox danger-full-access`
- Observed first status line: `HTTP/2 200`
- Result: PASS

## Manual TUI Checks

Interactive launch executed with:

- `codex-vl --no-alt-screen --dangerously-bypass-approvals-and-sandbox`

Observed:

- First launch rendered the startup surface with version `v0.142.0`, active model `gpt-5.5 high`, current workspace directory, and permission mode `YOLO mode`.
- MCP startup status rendered in the TUI.
- Vivling startup/status surface rendered and reached a ready state.
- The operator confirmed in-session that the TUI works.
- Interactive slash-command capture in this PTY remained ANSI-heavy and animated, so `/loop` and `/goal` were validated at the AI tool level instead of relying on a noisy TUI transcript.
- The TUI was shut down after the check; no interactive test process was left running.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified by runtime listing smoke in `<tmp-workspace>` under `danger-full-access`.
- Runtime/write surface: verified by creating and reading `hello.txt` under `danger-full-access`.
- Network surface: verified by the HTTP status-line smoke under `danger-full-access`.
- Patch surface: this Linux report file was created via the patch workflow.

Post-checks required after report creation:

- `git diff --check`
- `git status --short`

Result: PASS

## Failures, Blockers, Residual Risk

- Environmental limitation: Linux `workspace-write` sandbox smokes fail on this host before command execution with `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`.
- Environmental limitation: loop management add created the test loop enabled despite `enabled=false`; the loop was removed immediately and final loop list was empty.
- Environmental limitation: TUI PTY capture is not clean enough for reliable slash-command transcripts in this environment.
- No core command-surface, installed-package, TUI startup, read/write, or network regression was found outside the host sandbox blocker.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed Linux package version and command surface are correct; AI-manual runtime read, write, and network smokes pass under `danger-full-access`; TUI startup/Vivling were manually confirmed; goal and loop tool surfaces were tested directly in the validating AI session; the remaining failure is the known host `bwrap` loopback limitation affecting `workspace-write`.
