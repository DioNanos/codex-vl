# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-06-16
- Platform: macOS
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.140.0`
- Commit under test: `56df0c5a96bbedede9afc472d2ad767dabc13860`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and tracks `origin/develop`.
- HEAD under test is `56df0c5a96` (`release: bump @mmmbuto/codex-vl to 0.140.0`).
- Installed binaries report `0.140.0`.
- `npm dist-tags` show `stable: 0.135.0`, `latest: 0.139.0`, and `next: 0.140.0`, with matching platform-specific `*-next` tags.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from the installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.140.0`
- `codex-vl-exec --version` -> `codex-exec 0.140.0`
- Major help routes resolved successfully.
- `codex-vl completion bash` generated a completion file successfully.
- `codex-vl login status` returned a readable logged-in state without exposing credentials.
- `codex-vl mcp list` showed configured servers with secrets redacted.
- `codex-vl features list` exposed the expected feature inventory, including goal and unified-exec surfaces.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temporary workspaces:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Non-interactive exec returned `OK`.
- The directory/listing smoke returned the temporary workspace path and `seed.txt`.
- The write/read smoke succeeded and returned:
  - `seed`
  - `hello-codex-vl`
- The network smoke did not return an HTTP status line and failed with:
  - `curl: (6) Could not resolve host: www.google.com`

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed behavior:

- First launch rendered the startup surface with version `v0.140.0`, current workspace directory, and permission mode `YOLO mode`.
- The TUI reached a stable `ready` state after startup.
- The session was terminated cleanly after the checks.

Limitation:

- Slash-command surfaces such as `/loop`, `/goal`, `/vivling`, and `/vl` were hard to verify from the raw PTY capture because startup animation and ANSI redraws obscured the output. I am not claiming full slash-command verification from this capture alone.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via `pwd` and `ls` in a temporary workspace.
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback under `workspace-write`.
- Patch surface: this report file was added via the patch workflow and is the only intended repo change.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- The network smoke could not resolve `www.google.com`, so the HTTP status line check was blocked by DNS rather than by Codex.
- The TUI capture is noisy in this terminal, so interactive slash-command behavior is only partially observable from plain text.
- Startup emitted a plugin hooks config parse warning about `security-guidance/2.0.6/hooks/hooks.json`; it did not block the requested checks.
- No core command-surface or installed-runtime regression was found.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed `0.140.0` build passed the repo/release checks, the command surface, the non-interactive exec path, the workspace-write runtime smoke, and the report patch workflow. The remaining failures are host/DNS/PTy limitations, not product regressions.
