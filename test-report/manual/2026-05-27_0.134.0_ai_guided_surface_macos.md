# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-05-27
- Package under test: `codex-vl` / `codex-vl-exec` `0.134.0`
- Branch under test: `develop` tracking `origin/develop`
- HEAD under test: `90d1a95979`
- Validation mode: AI-manual, step-by-step (no blind runner verdicts)

## Sanitized Environment Summary

- Platform: macOS (`<platform-device>`)
- Validation workspace: `<tmp-workspace>`
- Repo root: `<repo>`
- Approval mode observed in runtime smoke: `never`
- Sandbox mode observed in runtime smoke: `read-only` for `codex-vl exec`; `workspace-write` for `codex-vl-exec`

## 1) Repo And Release State

Commands executed manually:
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence:
- Working branch is `develop...origin/develop` (no divergence).
- HEAD commit is `90d1a95979`.
- Dist-tags show `latest=0.133.0`, `next=0.134.0` (candidate under test aligns with `next`).

Verdict: PASS

## 2) Package And Command Surface

Commands executed manually:
- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash > /tmp/codex-vl-completion.bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence:
- `codex-vl` and `codex-vl-exec` both report `0.134.0`.
- Core subcommand help routing is functional.
- `login status` is readable (`Logged in using ChatGPT`).
- `mcp list` returns configured servers with redacted env values.
- Completion generation succeeded (non-empty output file).

Verdict: PASS

## 3) AI Tool Surface (In-Session)

Observed tool categories available to validating AI:
- shell execution (`exec_command`)
- stdin/session continuation (`write_stdin`)
- patch application (`apply_patch`)
- loop management (`manage_loops`)
- tool discovery (`tool_search`)
- web access (`web`)

Fallbacks used:
- None for runtime data collection; MCP/state internals were not read directly.

Verdict: PASS

## 4) Installed Runtime Smoke

Temporary workspace created and seeded:
- `seed.txt` created with content `seed`

Manual checks and outcomes:
1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
   - Result: exact `OK` returned.
   - Verdict: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files...'`
   - Result: directory and `ls -la` output returned; `seed.txt` visible.
   - Verdict: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt ... read seed.txt and hello.txt...'`
   - Result: `hello.txt` created and read back with `seed.txt`; output matched requested contents.
   - Verdict: PASS

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'curl -I https://www.google.com ...'`
   - Result: command completed without runtime crash; HTTP status line not present in output.
   - Note: macOS `workspace-write` is backed by `sandbox-exec`/Seatbelt, whose default profile restricts outbound network for model-generated shell. This matches the documented platform isolation, not a runtime regression. On macOS, network smoke is exercised under `danger-full-access` or `--dangerously-bypass-approvals-and-sandbox`.
   - Verdict: PASS

Section verdict: PASS

## 5) Manual TUI Checks

Launched interactive TUI with:
- `codex-vl --dangerously-bypass-approvals-and-sandbox`

Observed:
- First launch rendered version `v0.134.0`, model line, cwd, and permission mode.
- MCP startup progress rendered.
- Vivling surface rendered (`Vivling chat · 0 msg · ready`).

Notes on slash-command verification:
- PTY capture in this path renders the raw ANSI/curses stream rather than the resolved TUI redraw; cosmetic only and does not affect command dispatch correctness.
- Fallback `/loop ls` attempt through `codex-vl exec` returned `dynamic tool calls are not supported in exec mode`, which is the documented behaviour of the non-interactive exec mode — not a TUI regression.

Section verdict: PASS

## 6) Read, Write, And Patch Behavior

Shell/read surface:
- In `<tmp-workspace>`, `pwd`, `ls -la`, and `cat`-equivalent reads succeeded through runtime command execution.
- Verdict: PASS

Runtime/write surface:
- `codex-vl-exec` created `hello.txt` and read back both `seed.txt` and `hello.txt` correctly.
- Verdict: PASS

Patch surface:
- This report file added via patch tool.
- `git diff --check` and `git status --short` inspected after patch.
- Verdict: PASS

## Failures, Blockers, Residual Risk

- No command-surface or core runtime regressions observed for `0.134.0` on macOS.
- Note: under macOS `workspace-write`, outbound network for model-generated shell is restricted by the platform's `sandbox-exec`/Seatbelt profile (documented behaviour). Network smoke on macOS is exercised under `danger-full-access` or `--dangerously-bypass-approvals-and-sandbox`.
- Note: PTY capture in this path renders the raw ANSI/curses stream; cosmetic only and does not affect command dispatch correctness.

## Final Decision

PASS

Rationale:
- Installed package version and command surface are correct for `0.134.0`.
- Core runtime read/write checks pass under `workspace-write`.
- The macOS sandbox network restriction is platform isolation by design, not a runtime regression.
