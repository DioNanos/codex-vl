# Codex-VL AI-Guided Surface Report (macOS)

- Date: 2026-06-09
- Platform: macOS (Apple Silicon)
- Validation mode: AI-manual (command-by-command), not runner-driven
- Package under test: `codex-vl` / `codex-vl-exec` `0.138.0`
- Commit under test: `984e66158f`
- Branch under test: `develop` tracking `origin/develop`

## 1. Repo And Release State

Checks executed:

- `git fetch --all --prune`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Evidence summary:

- Local branch is `develop` and is aligned with `origin/develop`.
- HEAD under test is `984e66158f` (`chore: bump npm package to 0.138.0 and refresh release-channel README`).
- Installed binaries report `0.138.0`.
- `npm dist-tags` show `latest` at `0.137.0` and `next` at `0.138.0`, which matches the release candidate staged on `develop`.

Verdict: PASS.

## 2. Package And Command Surface

Checks executed from the installed commands:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Evidence summary:

- `codex-vl --version` -> `codex-cli 0.138.0`
- `codex-vl-exec --version` -> `codex-exec 0.138.0`
- Major help routes resolved successfully.
- `codex-vl completion bash` generated a completion script successfully.
- `codex-vl login status` returned a readable logged-in state without exposing secrets.
- `codex-vl mcp list` showed configured servers with sensitive values redacted.
- `codex-vl features list` exposed the expected feature inventory.

Verdict: PASS.

## 3. AI Tool Surface

Tool categories observed in-session:

- shell command execution
- stdin/session continuation
- patch application
- plan management

Verdict: PASS.

## 4. Installed Runtime Smoke

Executed manually in disposable temporary workspaces:

- `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`

Evidence summary:

- Exact `OK` response received from the non-interactive exec path.
- Directory/read listing returned the expected temporary workspace contents, including `seed.txt`.
- Write/read test succeeded with `seed` and `hello-codex-vl` returned in order.
- Network check did not return an HTTP status line; `curl` failed with DNS resolution error for `www.google.com`.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 5. Manual TUI Checks

Interactive launch check executed:

- `codex-vl --dangerously-bypass-approvals-and-sandbox --no-alt-screen`

Observed behavior:

- First launch rendered the startup surface with version `v0.138.0`, current workspace directory, and permission mode `YOLO mode`.
- MCP servers started and the UI reached a ready state without crashing.
- The session was exited cleanly.

Verdict: PASS WITH ENVIRONMENTAL LIMITATION.

## 6. Read, Write, And Patch Behavior

Coverage:

- Shell/read surface: verified via `pwd` and `ls` in temporary workspaces.
- Runtime/write surface: verified via `codex-vl-exec` file creation and readback.
- Patch surface: this report file was added via the patch workflow and is the only intended repo change.

Post-checks:

- `git diff --check`
- `git status --short`

Verdict: PASS.

## Failures, Blockers, Residual Risk

- Network smoke is blocked by host DNS resolution in this environment.
- The interactive TUI capture is noisy in plain text, but the launch itself was stable and versioned correctly.

## Final Decision

PASS WITH ENVIRONMENTAL LIMITATION

Rationale: the installed `0.138.0` build passed the command surface, runtime smoke, and TUI launch checks on macOS; the only failures were environmental DNS resolution during the network smoke and noisy terminal capture for interactive UI details.
