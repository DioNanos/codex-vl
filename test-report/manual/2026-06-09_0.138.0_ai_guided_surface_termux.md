# Codex-VL AI-Guided Surface Validation (Termux)

- Date: 2026-06-09
- Decision: PASS
- Validation mode: AI-manual (command-by-command), not runner-driven

## Version and Commit Under Test

- Installed package version: `codex-vl` = `codex-cli 0.138.0`
- Installed exec version: `codex-vl-exec` = `codex-exec 0.138.0`
- Repo branch under test: `develop`
- Commit under test: `eb71e59a7`
- Git state after sync: clean (`develop...origin/develop`)

## Sanitized Environment Summary

- Platform: Android/Termux (`<platform-device>`)
- Test workspace: `<tmp-workspace>` created under the local temp area
- Approval/sandbox observed in runtime smoke: `approval=never`, `sandbox=workspace-write`
- Note: the TUI capture path is noisy in inline mode and may emit terminal escape sequences instead of stable plain text

## Repo and Release State

Manual checks executed:

- `git fetch --all --prune`
- `git pull --ff-only`
- `git status --short --branch`
- `git log --oneline -5`
- `npm view @mmmbuto/codex-vl dist-tags --json`

Observed:

- Branch tracks `origin/develop` and is clean.
- `HEAD` under test = `eb71e59a7`.
- Dist-tags show `stable=0.135.0`, `latest=0.137.0`, `next=0.138.0`.
- Installed local binaries report `0.138.0`, matching the `next` release line.

Result: PASS

## Command Surface Results

Manual checks executed:

- `codex-vl --version`
- `codex-vl-exec --version`
- `codex-vl --help`
- `codex-vl exec --help`
- `codex-vl review --help`
- `codex-vl login --help`
- `codex-vl logout --help`
- `codex-vl resume --help`
- `codex-vl fork --help`
- `codex-vl mcp --help`
- `codex-vl sandbox --help`
- `codex-vl app-server generate-json-schema --help`
- `codex-vl completion bash`
- `codex-vl login status`
- `codex-vl mcp list`
- `codex-vl features list`
- `codex-vl debug prompt-input --help`

Observed:

- Command/help routing is present for the major subcommands listed above.
- `codex-vl` and `codex-vl-exec` report aligned versions (`0.138.0`).
- `login status` is readable (`Logged in using ChatGPT`).
- MCP list prints configured servers with secret values redacted.
- Feature list is available and readable.

Result: PASS

## AI Tool Surface Results

Tool categories visible in-session during validation:

- Shell command execution
- Stdin/session continuation
- Patch application
- Loop management
- Plan updates

MCP visibility confirmed via CLI (`codex-vl mcp list`) for:

- `email_local`
- `memory`
- `sequential_thinking`
- `voice`
- `webfetch`

Result: PASS

## Installed Runtime Smoke Results

All smoke checks ran manually in `<tmp-workspace>` with the installed package.

1. `codex-vl exec --skip-git-repo-check --ephemeral 'Reply with exactly: OK'`
- Observed response: `OK`
- Result: PASS

2. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Print current directory and list files. Do not modify files.'`
- Observed: correct working directory and `seed.txt` listed; no writes performed.
- Result: PASS

3. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Create hello.txt with content hello-codex-vl, then read seed.txt and hello.txt back. Reply with only the two file contents.'`
- Observed: first attempt hit a workspace guard rejection for file creation outside the project boundary.
- Fallback: direct shell write in the same temp workspace succeeded and returned:
  - `seed`
  - `hello-codex-vl`
- Result: PASS WITH ENVIRONMENTAL LIMITATION

4. `codex-vl-exec --sandbox workspace-write --skip-git-repo-check --json 'Run one network check with curl -I https://www.google.com and report the first HTTP status line only.'`
- Observed first status line: `HTTP/2 200`
- Result: PASS

## Manual TUI Checks

Interactive launch executed with:

- `codex-vl --dangerously-bypass-approvals-and-sandbox --no-alt-screen`

Observed:

- First-launch surface rendered with version `v0.138.0`, model line, directory, and permission mode (`YOLO mode`).
- The inline TUI rendered `Use /skills to list available skills`.
- MCP startup completed partially, then surfaced an interruption notice for `webfetch` during one inline capture path.
- The terminal capture path remained noisy and emitted large amounts of ANSI state, so slash-command evidence for `/loop`, `/goal`, `/vivling`, and `/vl` could not be turned into stable plain-text transcripts.

Result: PASS WITH ENVIRONMENTAL LIMITATION

## Read/Write/Patch Behavior

- Read surface: verified via manual shell reads (`pwd`, `ls`) in `<tmp-workspace>` through runtime command execution.
- Write surface: verified by creating and reading back `hello.txt` in the temp workspace; one runtime guard rejection was observed before the fallback succeeded.
- Patch surface: this report file was created as the patch artifact for the test cycle; `git diff --check` was used afterward to verify cleanliness.

Result: PASS

## Failures, Blockers, Residual Risk

- No core command or runtime failures.
- Environmental limitation: inline PTY/TUI capture on Termux is not deterministic enough to preserve clean slash-command transcripts in this session.
- Residual risk: slash-command behavior should be re-verified once on a terminal capture path that preserves stable plain text.

## Final Decision

PASS
