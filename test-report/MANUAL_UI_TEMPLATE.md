# Manual UI Report

**Goal**: interactive validation of `codex-vl` behavior that cannot be trusted
to unattended runtime checks

## Target

- platform:
- package version:
- test date:

## Manual checks

- `PASS` or `FAIL` first interactive launch
- `PASS` or `FAIL` thread start/resume/fork in TUI
- `PASS` or `FAIL` `/loop` interaction in TUI
- `PASS` or `FAIL` `/vivling` interaction
- `PASS` or `FAIL` `/vl` interaction
- `PASS` or `FAIL` visual/UX sanity

## Notes

-

## Screenshots

-

## Release recommendation

- `GO` or `NO-GO`
- rationale:
