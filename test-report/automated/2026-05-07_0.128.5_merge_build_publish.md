# Automated Merge, Build, and Publish Report - 0.128.5

**Goal**: merge current upstream into Codex VL `develop`, preserve `/vivling`,
`/loop`, `/goal`, and Termux packaging patches, validate optimized release
payloads, and publish npm `next`.

## Status

- release version: `0.128.5`
- branch: `develop`
- merge commit: `f4ed521592`
- version bump commit: `d04007aa3f`
- upstream merged: `upstream/main` at `9417cf9696`
- current V8 crate: `146.4.0`
- Linux x64: `PASS`
- Termux Android arm64: `PASS`
- macOS arm64 source-build package: `PASS`
- npm publish: `PASS`
- npm install from `next`: `PASS`
- Forge push: `PASS`
- Rusty V8 `147.4.0` readiness: `PASS` for Termux Android artifacts

## Linux x64

Build profile:

- target: `x86_64-unknown-linux-musl`
- `opt-level=3`
- `lto=fat`
- `codegen-units=1`
- `panic=abort`
- `strip=symbols`
- `CARGO_BUILD_JOBS=2`

Validation:

- `PASS` rusty_v8 `146.4.0` Linux musl archive checksum verification
- `PASS` `codex-bwrap` release build
- `PASS` `codex-cli` release build with `CODEX_BWRAP_SHA256`
- `PASS` `codex-exec` release build
- `PASS` `codex-rs/target/x86_64-unknown-linux-musl/release/codex --version`
  returned `codex-cli 0.128.5`
- `PASS` Linux npm tarball staging
- `PASS` temporary npm install of Linux platform tarball
- `PASS` installed Linux tarball binary returned `codex-cli 0.128.5`

## Termux Android arm64

Build profile:

- target: `aarch64-linux-android`
- NDK: `android-ndk-r27c`
- `opt-level=3`
- `lto=fat`
- `codegen-units=1`
- `panic=abort`
- `strip=symbols`
- `CARGO_BUILD_JOBS=2`

Validation:

- `PASS` rusty_v8 `146.4.0` Android artifact resolution from the codex-termux
  release mirror
- `PASS` `codex-cli` release build
- `PASS` `codex-exec` release build
- `PASS` final Android binaries are AArch64 PIE executables with
  `/system/bin/linker64`
- `PASS` final Android binaries are stripped
- `PASS` Android npm tarball staging

## macOS arm64

- `PASS` macOS arm64 source-build npm payload staged
- macOS remains a local Cargo postinstall package rather than a prebuilt native
  binary package.

## npm Publish

Published versions:

- `@mmmbuto/codex-vl@0.128.5`
- `@mmmbuto/codex-vl@0.128.5-linux-x64`
- `@mmmbuto/codex-vl@0.128.5-android-arm64`
- `@mmmbuto/codex-vl@0.128.5-darwin-arm64`

Verified dist-tags after publish:

```json
{
  "latest": "0.128.2",
  "next": "0.128.5",
  "linux-x64": "0.128.5-linux-x64",
  "android-arm64": "0.128.5-android-arm64",
  "darwin-arm64": "0.128.5-darwin-arm64"
}
```

Temporary npm install from `@mmmbuto/codex-vl@next` returned
`codex-cli 0.128.5`.

Artifacts staged locally under `.artifacts/npm-release-0.128.5/` are ignored by
git and are not part of the repository payload.

## Focused Merge Tests

Already run during the merge gate:

- `PASS` `cargo check --manifest-path codex-rs/Cargo.toml -p codex-tui`
- `PASS` `cargo test --manifest-path codex-rs/Cargo.toml -p codex-state thread_loop -- --nocapture`
- `PASS` `cargo test --manifest-path codex-rs/Cargo.toml -p codex-core goal_tool -- --nocapture`
- `PASS` `cargo test --manifest-path codex-rs/Cargo.toml -p codex-tui vivling -- --nocapture`
- `PASS` `cargo check --manifest-path codex-rs/Cargo.toml -p codex-cli`

## Notes

- The first Linux build completed, but `CODEX_BWRAP_SHA256` was empty due to a
  shell quoting error in the ad hoc build command. The final Linux `codex`
  binary was rebuilt with the correct `bwrap` SHA before packaging.
- Rusty V8 `147.4.0` is not part of the current baseline build yet. It remains
  a separate alignment lane because this release build intentionally stays on
  V8 `146.4.0`.
- Rusty V8 `147.4.0` Linux musl assets are available from `openai/codex`.
  The matching codex-termux Android assets were source-built on the release runner from
  `denoland/rusty_v8` tag `v147.4.0`, published to
  `DioNanos/codex-termux` release `rusty-v8-v147.4.0`, and recorded in
  `third_party/v8/android-artifacts.toml`.
- `PASS` Rusty V8 `147.4.0` Android archive asset digest:
  `d33850bbe8e351a9c4ee0fdd679bd11cb9a4fa0761415809e894010128994e13`
- `PASS` Rusty V8 `147.4.0` Android binding asset digest:
  `09dee789aaf77fb3d5dcec556a62e78a342cc4a02f8cf36e0f48542f7645bf39`
- `PASS` manifest-driven download verification for both Rusty V8 `147.4.0`
  Android assets.
