# Codex VL 0.129.0 Linux Post-Install Suite

**Date**: 2026-05-07
**Suite type**: AI-guided Linux post-install validation
**Decision**: PASS

## Scope

Validate the published and locally installed Linux `0.129.0` release after the
final upstream `rust-v0.129.0` merge. This pass focuses on Linux command
surface, fork-specific Rust tests, npm dist-tags, sanitized reporting, and
read/write behavior.

This report is sanitized for public mirroring. Local paths, hostnames, process
details, and raw logs are intentionally omitted.

## Release State

- Branch under test: `develop`
- Commit under test: `8330b68c24`
- Upstream merge present: `rust-v0.129.0`
- Installed command: `codex-vl`
- Installed version: `codex-cli 0.129.0`
- Installed exec version: `codex-exec 0.129.0`
- Forge develop state: local `develop` matched `origin/develop` before this
  report commit.
- Working tree before report: tracked files clean; local build artifacts left
  untracked.

## npm State

`@mmmbuto/codex-vl` was verified on npm:

- `latest`: `0.129.0`
- `linux-x64`: `0.129.0-linux-x64`
- `android-arm64`: `0.129.0-android-arm64`
- `darwin-arm64`: `0.129.0-darwin-arm64`
- `next`: `0.128.5`

During this validation pass, the platform dist-tags were found still pointing
at the previous `0.128.5-*` versions and were corrected to `0.129.0-*`.

## Command Surface

The installed Linux package passed the runtime command surface suite:

- Overall: PASS
- Passed checks: 21
- Failed checks: 0
- Skipped checks: 3

Covered command surfaces:

- version
- top-level help
- exec help
- review help
- login/logout help
- resume/fork help
- mcp help/list
- sandbox help
- app-server help
- completion generation
- login status
- features list
- schema generation help and output
- Node wrapper help, exec help, review help, and resume help

The reusable runtime script looks for the upstream `codex-exec` command name,
so its exec check was skipped in that script. The fork command
`codex-vl-exec` was checked separately and returned `codex-exec 0.129.0`.

Live model-driven exec smokes were not run in this pass. This Linux pass is a
post-install local/runtime validation; model-backed manual validation remains a
separate operator gate before public GitHub release promotion.

## Focused Rust Tests

All focused fork tests passed:

- `codex-core goal_tool`: 5 passed
- `codex-core spec_plan`: 42 passed, 1 ignored
- `codex-tui loop_controller`: 8 passed
- `codex-tui vivling::runtime`: 93 passed
- `codex-state thread_loop_jobs`: 2 passed
- `codex-state goals`: 18 passed

Coverage confirmed:

- goal tools and goal feature gating
- goal replacement, completion, pause, clear, budgeting, and accounting
- dynamic tool registration for flat and namespaced `manage_loops`
- loop controller behavior
- loop state persistence
- Vivling runtime behavior
- Vivling MSA relevant-memory recall

## Linux Build Evidence

The previously produced optimized Linux build remains green:

- target: Linux x64 musl
- version check: `codex-cli 0.129.0`
- `codex`, `codex-exec`, and `bwrap` checksums were produced during the final
  build.

The Android build order gate was also preserved: Android started only after the
Linux build was green. Android is not the focus of this Linux post-install
suite, but the release state was not regressed.

## Read, Write, And Patch Behavior

Read/write behavior passed in a temporary workspace:

- created a seed file
- read the seed file back
- created a second file
- read the second file back
- listed the expected files

Patch behavior is covered by this report update itself. The report was added
through the agent patch tool, then inspected with git status and diff checks
before commit.

## Manual TUI Status

Manual TUI testing was not repeated in this post-install Linux pass. Earlier
AI-guided reports cover the interactive `/loop`, `/goal`, and Vivling surfaces.
For public GitHub release promotion, the remaining operator gate is a fresh
manual test-report approval.

## Blockers

No Linux post-install blockers remain.

Remaining non-Linux release gate:

- wait for operator test-report approval before GitHub `main` promotion and
  public GitHub release creation.

## Sanitization

This report intentionally avoids:

- absolute local paths
- private hosts or IP addresses
- usernames and account identifiers
- tokens or environment dumps
- raw process lists
- unredacted MCP configuration
