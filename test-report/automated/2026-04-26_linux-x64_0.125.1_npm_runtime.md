# Automated Runtime Report - Linux x64 npm 0.125.1

**Goal**: unattended validation of the installed `codex-vl` package

## Target

- platform: Linux x64
- package version: `@mmmbuto/codex-vl@0.125.1`
- install source: npm registry, clean temporary global prefix, then clean local npm global prefix
- test date: 2026-04-26

## Status

- `PASS` package/version check
- `PASS` subcommand help routing
- `PASS` installed binary/package sanity
- `PASS` clean local npm global install
- `PASS` npm registry metadata and dist-tags
- `PASS` release tarball payload validation
- `PASS` no-crash CLI startup surface for `--version` and `codex-vl-exec --help`
- `PASS` Ollama service enabled and active at boot target
- `PASS` Ollama API smoke
- `PASS` custom/Ollama `codex-vl exec` smoke with `deepseek-v4-flash:cloud`
- `PASS` flat `manage_loops` visibility to DeepSeek via custom/Ollama model
- `PASS` DeepSeek attempted `manage_loops` tool invocation twice
- `N/A` `manage_loops` execution in `codex exec`; dynamic tool calls are rejected in exec mode by design/runtime
- `NOT RUN` interactive `/loop` TUI lifecycle
- `NOT RUN` login status, MCP listing, feature listing, completion generation, app-server schema generation, workspace write/read, and network smoke

## Commands or checks run

- `npm view @mmmbuto/codex-vl version dist-tags versions --json`
- `npm install -g --prefix /tmp/codex-vl-npm-smoke-* @mmmbuto/codex-vl@0.125.1`
- `/tmp/codex-vl-npm-smoke-*/bin/codex-vl --version`
- `/tmp/codex-vl-npm-smoke-*/bin/codex-vl-exec --help`
- `find /tmp/codex-vl-npm-smoke-*/lib/node_modules -type f \( -name codex -o -name codex-exec -o -name 'libc++_shared.so' \)`
- remove previous local `codex-vl` and `codex-vl-exec` aliases
- remove previous local manual install directory
- `npm install -g --prefix <local npm prefix> @mmmbuto/codex-vl@0.125.1`
- `<local npm prefix>/bin/codex-vl --version`
- `<local npm prefix>/bin/codex-vl-exec --help`
- `systemctl is-enabled ollama.service`
- `systemctl is-active ollama.service`
- `curl -fsS http://localhost:11434/api/tags`
- `codex-vl exec --oss --local-provider ollama -m deepseek-v4-flash:cloud ...`
- `codex-vl exec --oss --local-provider ollama -m deepseek-v4-flash:cloud "Inspect your available tools ... manage_loops ..."`
- `codex-vl exec --oss --local-provider ollama -m deepseek-v4-flash:cloud "Use the manage_loops tool ..."`

## Result

- summary: npm `latest` resolves to `0.125.1`, clean install succeeds, `codex-vl --version` reports `codex-cli 0.125.1`, and `codex-vl-exec --help` resolves to the native exec help.
- local npm global install:
  - `codex-vl` resolves to the npm-managed `bin/codex.js`
  - `codex-vl-exec` resolves to the npm-managed `bin/codex-exec.js`
  - `npm list -g --prefix <local npm prefix> --depth=1 @mmmbuto/codex-vl -> @mmmbuto/codex-vl@0.125.1`
- npm dist-tags:
  - `latest -> 0.125.1`
  - `linux-x64 -> 0.125.1-linux-x64`
  - `darwin-arm64 -> 0.125.1-darwin-arm64`
  - `android-arm64 -> 0.125.1-android-arm64`
- installed Linux payload:
  - `@mmmbuto/codex-vl-linux-x64/vendor/x86_64-unknown-linux-musl/codex/codex`
  - `@mmmbuto/codex-vl-linux-x64/vendor/x86_64-unknown-linux-musl/codex/codex-exec`

## Release artifact validation

- macOS ARM64 tarball contains `codex` and `codex-exec`
- Linux x64 tarball contains `codex` and `codex-exec`
- Android ARM64 tarball contains `aarch64-linux-android/codex/codex`, `aarch64-linux-android/codex/codex-exec`, and `aarch64-linux-android/codex/libc++_shared.so`

## Notes

- `@mmmbuto/codex-vl@0.125.0` is deprecated in npm.
- Previous manual local symlinks to a `0.125.0` build were removed and replaced by npm-managed symlinks.
- Ollama system service was repaired and is `enabled` and `active`.
- `codex-vl exec` through Ollama succeeds with `deepseek-v4-flash:cloud` and returns the expected response.
- DeepSeek via Ollama explicitly reported seeing `manage_loops`.
- DeepSeek attempted to call `manage_loops` with `action=list` twice.
- In `codex exec`, the custom/Ollama model can see and attempt flat `manage_loops`, but the runtime rejects execution with `dynamic tool calls are not supported in exec mode`. Full `/loop` lifecycle validation remains a TUI check.
