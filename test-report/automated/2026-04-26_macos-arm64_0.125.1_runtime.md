# Automated Runtime Report - macOS arm64 0.125.1

**Goal**: unattended validation of the installed `codex-vl` package on the Mac

## Target

- platform: macOS arm64
- package version: `codex-cli 0.125.1`
- install source: installed binary at `/opt/homebrew/bin/codex-vl`
- test date: 2026-04-26

## Status

- `PASS` runtime suite overall
- `PASS` version check
- `PASS` top-level help
- `PASS` `exec` help
- `PASS` `review` help
- `PASS` `login` help
- `PASS` `logout` help
- `PASS` `resume` help
- `PASS` `fork` help
- `PASS` `mcp` help
- `PASS` `sandbox` help
- `PASS` `app-server` help
- `PASS` completion generation
- `PASS` login status
- `PASS` MCP listing
- `PASS` features listing
- `PASS` app-server schema generation
- `PASS` node wrapper help
- `PASS` node wrapper `exec` help
- `PASS` node wrapper `review` help
- `PASS` node wrapper `resume` help
- `SKIP` `codex-exec` version
- `SKIP` installed binary linkage
- `PASS` live `exec` ephemeral smoke
- `SKIP` live `codex-exec` smokes
- `FAIL` loop-in-exec attempt

## Commands or checks run

- `qa/runtime-suite/run_cli_runtime_suite.sh --live`
- `/opt/homebrew/bin/codex-vl --version`
- `/opt/homebrew/bin/codex-vl exec --skip-git-repo-check --ephemeral "Reply with exactly: OK"`
- `/opt/homebrew/bin/codex-vl exec --skip-git-repo-check --ephemeral "Use manage_loops to create a temporary loop named mac-test-loop, list it, then remove it. If tool calls are unsupported in exec mode, state that clearly."`

## Result

- summary: the installed Mac `codex-vl` runtime is healthy for the safe CLI surface and live `exec` smoke. The loop-in-exec test fails because dynamic tool calls are not supported in `exec` mode for this thread.
- runtime suite report was retained locally for validation.
- loop test result:
  - the model attempted to create and manage a temporary loop
  - the runtime rejected the request with `dynamic tool calls are not supported in exec mode`
  - no loop was created, listed, or removed

## Notes

- The `exec` path is valid for standard non-interactive smoke testing.
- `manage_loops` remains available in interactive surfaces, but not in this `exec` context.
- The runtime suite output was generated on the local Mac install.
