# Automated Runtime Report - Termux 0.125.1

**Goal**: unattended + targeted loop-aware validation of the installed `codex-vl` package

## Target

- platform: termux
- package version: `0.125.1`
- install source: npm global install
- test date: 2026-04-26

## Status

- `PASS` package/version check
- `PASS` top-level help
- `PASS` subcommand help routing
- `PASS` login status
- `PASS` MCP listing
- `PASS` feature listing
- `PASS` completion generation
- `PASS` app-server schema generation
- `PASS` codex-exec availability
- `PASS` installed binary linkage check (skipped; native codex binary not present in npm vendor tree)
- `PASS` no-crash runtime surface (`--version`, command routing)
- `PASS` live exec ephemeral check (`codex-vl exec --skip-git-repo-check --ephemeral`)
- `PASS` workspace list-files smoke
- `PASS` workspace write-read smoke (manual re-run with explicit prompt)
- `PASS` network smoke
- `PASS` loop tool lifecycle check (`manage_loops` add/list/remove)
- `RESOLVED` initial live suite run `workspace write-read smoke` failed due malformed quoted prompt in the suite command, then passed after the prompt fix
- `NOT RUN` interactive `/loop` lifecycle in TUI (not executable from non-interactive runtime suite)

## Commands or checks run

- `bash qa/runtime-suite/run_cli_runtime_suite.sh`
- `bash qa/runtime-suite/run_cli_runtime_suite.sh --live`
- `codex-vl --version`
- `codex-vl --help`
- `codex-vl exec --skip-git-repo-check --ephemeral "Reply with exactly: OK"`
- `codex-exec --sandbox workspace-write --skip-git-repo-check --json "print current directory and list files"`
- `codex-exec --sandbox workspace-write --skip-git-repo-check --json "create hello.txt with content hello and read it back"`
- `codex-exec --sandbox workspace-write --skip-git-repo-check --json "run one network check with curl -I https://www.google.com and report the first HTTP status line only"`
- `manage_loops` (add/list/remove)

## Result

- Summary: suite runtime controls and command surface are healthy on Termux for `codex-vl@0.125.1`; one scripted live smoke command was initially malformed, then retested and passed. `manage_loops` is available and can be managed via tool action.
- Loop check result: temporary loop `codex-vl-test-loop` was added, listed, and removed successfully.
- Live suite artifacts were retained locally for validation.

## Notes

- `workspace write-read smoke` initially failed only because the command used a quoted token that broke the generated shell argument string inside the test command.
- The first suite output passed all checks except that one resolved item.
- The `manage_loops` command path is exercised through the Codex tool interface and is not the same as interactive TUI `/loop` controls.
