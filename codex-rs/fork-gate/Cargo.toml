[package]
name = "fork-gate"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Gate di sanificazione pre-publish per il fork codex-vl (test-only)."

[lib]
test = false
doctest = false
path = "src/lib.rs"